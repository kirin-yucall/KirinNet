version: "3.9"

services:
  user-node:
    build: .
    container_name: kirinnet-user-node
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8080
      - DATA_DIR=/app/data
      - KIRINNET_DOMAIN=node1.local
