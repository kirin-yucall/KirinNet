# KirinNet 引导期计数与共识时间戳（Consensus Counter）

> **Version:** 0.2（草案·附条件履约）
> **Date:** 2026-08-08
> **Status:** 草案 v0.2 · 节点 PM 附条件会签通过（CC-010，2026-08-08 23:45）· 两条件履约 · 待 X-QA 回归 + 节点 PM 联合关闭 · 单边定稿无效
> **Scope:** 引导期计数包语义、1000 万阈值验证、多点协商共识时间戳（N≥7 odd quorum + ≥⌈2N/3⌉ 共识）、fail-closed 收敛窗口（T=3 周期告警不强制 OFF）、开关传播（计数关闭后永不返回）、VDF 预留位
> **关联编号:** T8（计数与共识时间戳）/ T1（count 包信封承载，**引用 T1 草案·未会签**）/ T9（签名覆盖范围，**引用 T9 草案·未会签**）/ 9.3（VDF v3 暂存 / 共识时间戳）/ FR6（好友链泛洪承载）/ **KNET-CC-010（节点 PM 附条件会签，2026-08-08 23:45，两条件履约）**
> **承载关系:** count 包的 wire 格式（`query_id`/`nonce`/`forward_chain`/`signature`）由 T1 定义；本文件定义其**语义、阈值、共识时间戳、开关传播、VDF 预留**。
> **签名口径:** count 包内权重/计数字段在 T9 签名覆盖范围内时，引用 T9 §9.3.2，P-FLOOD 不重定义。

---

## 0. 草案状态与依据

| 项 | 说明 |
|---|---|
| 草案定位 | v0.1 定义引导期计数→阈值→多点协商共识时间戳→开关传播的完整时序与字段语义；VDF 为**预留位**（不定型，守「方向驱动、分阶段细化」红线） |
| 协议新增 | 节点 02 篇**当前无 count 包实现**（FR 表无此项，服务器角色 0% 实现，KNET-CC-001 已会签确认）；本文件为协议层为引导期防投毒**新增**计数泛洪机制 |
| 依据 | `DECISIONS.md` §0（引导期计数 v3 恢复：域名发布走计数泛洪防投毒，计数达 1000 万 → 多点协商共识时间戳 → 全网开启开关，不再返回计数包；计数仅限引导期）+ §0（VDF v3 暂存：共识时间戳保留，作为发布时序裁定依据，VDF 暂存待用） |
| 会签要求 | T8 为强制会签项，须经节点 PM 会签（KNET-CC）后方可标「定稿」 |
| 不越界 | VDF 算法参数不定型（守红线）；签名覆盖范围归 T9；count 包 wire 归 T1；CC↔TW 切换点（共识时间戳触发开关 → 计数关闭、信任接管）与 T3 共管 |

---

## 1. 设计动机：两套机理分明

KirinNet 用**两套机理**防投毒/防污染，本文件管**第一套（引导期，中心化式、一次性阀门）**：

| 机理 | 定位 | 时段 | 机制 | 文件 |
|---|---|---|---|---|
| **引导期计数（本文件 T8）** | 防投毒（域名发布投毒） | 引导期（一次性阀门） | 计数泛洪 → 1000 万阈值 → 共识时间戳 → 开关传播 → 计数关闭永不返回 | **本文件** |
| **运营期信任权重（T3）** | 防污染（社交信任） | 运营期（持续机理） | 信任权重（min 保守聚合 + -127 黑名单）去中心化防污染 | **T3** |

**切换点（共管）:** 共识时间戳触发全网开关 → 计数关闭（T8）→ 信任权重接管（T3）。切换点由双 PM 共管（KNET-COM-03），本文件定义触发条件与 fail-closed 默认值，不单边裁定切换时机。

**去中心化纪律:** 引导期计数是**一次性中心化式阀门**（计数达阈值后全网一致关闭），**不违反**「不依赖中心化权威」—— 它是引导期临时手段，运营期完全由去中心化信任权重（T3）接管。

---

## 2. 计数包语义（count payload）

### 2.1 节点本地计 1 还是回执聚合？（明确：**本地计数 + 回执聚合**）

**结论（草案）:** 采用**本地计数 + 回执聚合**双轨：

1. **本地计数:** 每个节点对自身发布的域名在本地计数 1（节点级计数器，防同节点重复发布投毒）。
2. **回执聚合:** 节点发布域名时通过 `count` 泛洪包向好友链扩散，好友回执（`answer` 类回传）聚合累计计数（跨节点计数）。
3. **全网累计:** 全网累计计数 = Σ（各节点本地计数 + 回执聚合），由多点协商共识（§4）达成一致。

**依据:** 节点 02 篇当前无此实现，本结论为协议新增；选「本地计数 + 回执聚合」而非「纯本地计 1」是为了让全网计数可在阈值验证时多点交叉核验（防单点伪造计数）。

### 2.2 count payload 字段（wire 归 T1，语义归本文件）

```
{
  "counter_value": <整数, 节点本地计数值或回执聚合累计值>,
  "timestamp": <Unix 秒, 计数生成时间>,
  "consensus_flag": <bool, false=计数中, true=已达共识阈值触发开关>,
  "vdf_proof": <Base64URL | null, VDF 预留位, 见 §6, 当前恒为 null>
}
```

- `counter_value`: 单调递增（同节点），跨节点由回执聚合累计。
- `consensus_flag`: 计数期恒为 `false`；达 1000 万阈值且多点协商达成共识时间戳后置 `true`，触发开关传播（§5）。
- `vdf_proof`: **预留位**（§6），当前恒 `null`，VDF 机制暂存待用（§0 决策）。

### 2.3 配额计入

`count` 包计入发起节点的**每日用户自主泛洪配额**（T1 §8，每日 3 次，超限 -127）；与 `publish` 共享配额池。系统类（`announce`/`query`/`answer`）不计入。

---

## 3. 1000 万阈值验证

### 3.1 阈值

- **全网累计计数达 1000 万（10^7）** → 触发多点协商共识时间戳（§4）。
- 阈值固定，不动态调整（防被操纵降低门槛）。

### 3.2 验证规则

| 规则 | 说明 |
|---|---|
| **多点交叉核验** | 任一节点宣称达阈值时，须有 **≥⌈2N/3⌉ 个独立节点**（**N ≥ 7，odd quorum 奇数仲裁集**，N=7 时 ≥5；详见 §4.2）的回执聚合累计 ≥ 1000 万 |
| **签名防伪造** | count 包签名（T9 覆盖范围含 `forward_chain` + 计数字段）防伪造计数 |
| **去重防重放** | 同 `query_id` + `nonce` 二次计数丢弃（T1 §5.2 + T9 §9.3.1 一次性约束） |
| **拉黑节点不计** | T3 黑名单（-127）节点的计数回执**不计入**累计（防恶意节点灌水） |

### 3.3 阈值未达的行为

计数期（`consensus_flag=false`）内：count 包正常泛洪聚合，节点正常累计；未达阈值前不触发开关。

---

## 4. 多点协商共识时间戳

### 4.1 共识时间戳的作用

- 计数达 1000 万阈值后，全网节点通过**多点协商**达成一个**共识时间戳 `T_consensus`**。
- `T_consensus` 作为**发布时序裁定依据**（§0 VDF 决策）：后续域名发布以 `T_consensus` 为基准裁定时序（用户裁定，非自动裁决）。
- `T_consensus` 也是**开关触发的时刻标记**（§5）。

### 4.2 共识达成（拜占庭容限 + N 下限）

- **多点协商:** 节点间交换计数累计 + 本地时间戳，达成多数一致（共识需 **≥⌈2N/3⌉ 节点一致**，N 为参与多点协商的独立节点数，拜占庭容限 f < N/3）。
- **N 下限（odd quorum，奇数仲裁集）:** **N ≥ 7**（2026-08-08 23:45 节点 PM 会签附条件 CC-010 补齐）。
  - **N 与 2/3 的关系:** 共识需 **≥⌈2N/3⌉ 节点一致**；N=7 时需 **≥5** 节点一致（⌈2×7/3⌉ = ⌈4.67⌉ = 5）。
  - **奇数仲裁集（odd quorum）:** N 取奇数避免平票死锁（偶数 N 下 2/3 边界易出现 N/2:N/2 分裂），同时满足 N≥7 保证拜占庭容限 f<N/3 在最小可行规模成立（N=7 → f≤2，可容忍 2 个拜占庭节点）。
  - **N<7 不启动开关（守安全）:** 若参与多点协商的独立节点数 N<7，**fail-closed 不触发开关**（计数继续、等待更多节点上线），即使表面达成 2/3 多数也不开启开关 —— 守 fail-closed 红线，防小规模合谋伪造共识时间戳提前触发开关。
- **最终一致:** 允许短暂不一致（网络延迟），通过泛洪收敛；最终全网收敛到同一 `T_consensus`。
- **防分叉:** 若多点协商无法达成 2/3 多数（拜占庭节点过多）或 N<7，**fail-closed 不触发开关**（计数继续，进入收敛窗口 §4.4 等待更多节点上线）—— 守安全 fail-closed 红线。

### 4.3 共识时间戳字段

共识达成后，节点在 count 包或独立的 `consensus_announce` 包中广播：

```
{
  "consensus_timestamp": <T_consensus, Unix 秒>,
  "threshold_met": true,
  "participant_count": <参与多点协商的节点数>,
  "signature": <Ed25519, 引用 T9>
}
```

### 4.4 Fail-closed 收敛窗口（T=3 周期，告警不强制 OFF）

> 本节为 2026-08-08 23:45 节点 PM 会签附条件 CC-010 补齐条款（条件②）。

- **触发条件:** 多点协商**无法达 2/3 多数**（拜占庭节点过多 / 节点在线不足 / N<7），即 §4.2 防分叉情形触发。
- **收敛窗口 T=3 周期:** 进入**收敛窗口**，窗口长度 **T=3 周期**（「周期」= 本草案定义的计数周期，即 count 包泛洪聚合一个完整轮次的时长）。
- **窗口内行为（告警但不强制 OFF）:**
  - **告警:** 节点持续发出「无法达 2/3 多数」告警（日志 / 监控），提示运维与节点社区关注。
  - **计数继续:** count 包正常泛洪聚合、节点正常累计，**不强制 OFF**（开关维持当前态，通常为 ON 计数中）。
  - **等待收敛:** 等待更多节点上线（提升 N 至 ≥7 且达成 2/3 多数后即可触发开关），允许网络逐步收敛到可共识状态。
- **超 T 仍无法达成:** 窗口超时（T=3 周期满）后若仍无法达成 2/3 多数，则**持续告警、开关维持当前态**（仍不强制 OFF，继续计数 + 等待）——不引入硬超时强制 OFF，守 fail-closed「宁可不开关也勿错误开关」红线。
- **窗口退出（共识恢复）:** 收敛窗口内一旦达成 2/3 多数且 N≥7 → 立即生成共识时间戳 `T_consensus`、触发开关 OFF（§5），收敛窗口结束。

---

## 5. 开关传播（计数关闭后永不返回）

### 5.1 开关语义

- 共识时间戳 `T_consensus` 达成后，全网开启**计数关闭开关（switch=OFF）**。
- **开关语义:** `switch=OFF` 后，节点**永不返回 count 包**（不再发送、不再响应 count、不再累计）。
- **单向性:** 开关是**单向的**（ON→OFF），一旦关闭永不重新开启（防回退投毒）。
- **接管:** 开关 OFF 后，防污染由运营期信任权重（T3）接管。

### 5.2 开关传播（最终一致）

| 规则 | 说明 |
|---|---|
| **泛洪传播** | 开关 OFF 状态通过 `consensus_announce` 泛洪传播（沿好友链，TTL ≤6，受 forward_chain/fanout 约束） |
| **持久化** | 节点本地持久化 `switch=OFF` 状态（重启不丢失，防重启回退） |
| **拜占庭容限** | 少数拜占庭节点拒绝传播开关 OFF → 被多数节点 isolate（不响应其 count，因全网已 OFF） |
| **最终一致** | 允许短暂不一致（部分节点未收到），通过后续泛洪收敛到全网 OFF |

### 5.3 Fail-closed 默认值

- **默认值显式标注:** 节点启动时若未知开关状态 → **默认 switch=ON（计数中）**（保守，继续计数防投毒）；已知 switch=OFF → 永不返回 count。
- **冲突处置:** 节点收到矛盾的开关状态（部分 ON 部分 OFF）→ **以共识时间戳 `T_consensus` 为准**（有 `T_consensus` 即 OFF），fail-closed 倾向 OFF。

---

## 6. VDF 预留位（不定型）

### 6.1 当前状态（暂存待用）

- VDF（Verifiable Delay Function）机制 **v3 暂存待用**（§0 决策）—— 共识时间戳保留作为发布时序裁定依据（用户裁定），**VDF 算法暂不引入**。
- count payload 的 `vdf_proof` 字段为**预留位**，当前恒 `null`。

### 6.2 预留目的（方向驱动，分阶段细化）

- 未来若需**强时序保证**（防共识时间戳被提前/延后操纵），可启用 VDF：节点对 `T_consensus` 计算 VDF proof，验证方校验 proof 证明时间戳经过了真实延迟计算。
- **不定型:** VDF 算法选型（如 VDFs based on repeated squaring / class groups）、参数（延迟时长）**不在本草案定型**，守「方向驱动、分阶段细化」红线，留波 2+ 预研。

### 6.3 不越界

- VDF 算法参数定型归 P-ARCH（涉及密码学选型）；本文件仅预留字段位 + 声明用途，不定型实现。

---

## 7. 时序自洽性（无死锁/无永久阻塞）

### 7.1 计数→阈值→共识→开关时序

```
[引导期] 节点发布域名
   │
   ├─ count 包泛洪（T1 信封，TTL/fanout/forward_chain 约束）
   │     │
   │     ├─ 各节点本地计数 + 回执聚合
   │     └─ 拉黑节点不计 / 签名防伪 / query_id+nonce 去重
   │
   ├─ 全网累计 ≥ 1000 万？
   │     │
   │     ├─ 否 → 继续计数（无阻塞，节点正常运营）
   │     └─ 是 → 多点协商共识时间戳 T_consensus
   │              │
   │              ├─ N ≥ 7（odd quorum）且 多数（≥⌈2N/3⌉）一致？
   │              │     │
   │              │     ├─ 是 → 开关 OFF（永不返回 count）→ T3 信任接管
   │              │     └─ 否（N<7 或未达 2/3 多数）→ fail-closed 进入收敛窗口（T=3 周期告警不强制 OFF，计数继续等待节点上线，§4.4）
   │              │
   │              └─ （无死锁：少数拜占庭不阻塞多数，多数达成且 N≥7 即 OFF）
   │
   └─ [运营期] switch=OFF，T3 信任权重防污染
```

### 7.2 无死锁保证

- **计数期无阻塞:** 未达阈值时节点正常运营（announce/query/answer/publish 照常），count 仅是并发累加。
- **多点协商无永久阻塞:** 若 2/3 多数无法达成（拜占庭过多）或 N<7，fail-closed 进入收敛窗口（§4.4，T=3 周期告警不强制 OFF，计数继续等待节点上线），**不永久阻塞**运营期（其他协议功能不受影响）。
- **开关单向无回退:** OFF 后永不 ON，防回退投毒，时序单调。

---

## 8. 自测门禁（交付前自查）

| # | 门禁项 | 本草案结论 |
|---|---|---|
| T8-1 | 计数→阈值→共识→开关时序自洽（无死锁/无永久阻塞） | ✅ §7（计数无阻塞 / 多点协商 fail-closed 不永久阻塞 / 开关单向） |
| T8-2 | VDF 字段为「预留」非「定型」（守红线） | ✅ §6（vdf_proof 恒 null，算法不定型，归 P-ARCH） |
| T8-3 | 节点本地计 1 还是回执聚合已明确 | ✅ §2.1（本地计数 + 回执聚合双轨） |
| T8-4 | 1000 万阈值验证多点交叉核验 | ✅ §3.2（多点交叉核验需 ≥⌈2N/3⌉ 独立节点，N≥7 odd quorum，对齐 §4.2） |
| T8-5 | 开关传播（计数关闭后永不返回）单向性 | ✅ §5（ON→OFF 单向，持久化，最终一致） |
| T8-6 | 签名覆盖范围引用 T9 不重定义 | ✅ count 包签名引用 T9 §9.3.2 |
| T8-7 | CC↔TW 切换点（共识时间戳触发开关 → 信任接管）与 T3 互引 | ✅ §1/§5（切换点共管，T3 接管） |
| T8-8 | `grep -ri aura` 零残留 | ✅ |
| T8-9 | 引用编号一致（T1/T3/T8/T9/9.3/FR6） | ✅ |
| T8-10 | **N 下限已补（CC-010 条件①履约）** | ✅ §4.2（N≥7 odd quorum 奇数仲裁集，共识需 ≥⌈2N/3⌉，N=7 时 ≥5；N<7 不启动开关守安全） |
| T8-11 | **Fail-closed 收敛窗口已补（CC-010 条件②履约）** | ✅ §4.4（T=3 周期告警不强制 OFF，计数继续等待节点上线，超 T 持续告警开关维持当前态） |

---

## 9. 待会签 / 待定稿项（标注「草案」）

| 项 | 归属 | 状态 |
|---|---|---|
| count 包签名覆盖（算法/字段） | T9 / P-ARCH | **引用 T9 草案·未会签** |
| count 包 wire 格式（query_id/nonce/forward_chain） | T1 / P-FLOOD | **引用 T1 草案·未会签** |
| 多点协商多数阈值（2/3 多数 = ≥⌈2N/3⌉ 节点） | T8 会签定稿 | **CC-010 节点 PM 附条件会签通过**（2026-08-08 23:45），条件① N≥7 下限已补（§4.2，本 v0.2 履约）；待 X-QA 回归 + 节点 PM 联合关闭 |
| VDF 算法选型/参数 | P-ARCH（波 2+） | 不定型，仅预留位 |
| CC↔TW 切换时机裁定 | 双 PM 共管（KNET-COM-03） | 不单边裁定 |
| N（多点协商参与节点数下限） | T8 会签定稿 | **CC-010 条件①履约**：N≥7 odd quorum（§4.2，本 v0.2 补齐） |
| Fail-closed 收敛窗口（T=3 周期） | T8 会签定稿 | **CC-010 条件②履约**：T=3 周期告警不强制 OFF（§4.4，本 v0.2 补齐） |

---

## 10. 版本与变更

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-08-08 | P-FLOOD 首版草案：计数包语义（本地计数 + 回执聚合，§2）+ 1000 万阈值多点交叉核验（§3）+ 多点协商共识时间戳 T_consensus（§4，拜占庭容限 f<n/3，2/3 多数建议）+ 开关传播（§5，ON→OFF 单向，永不返回 count，持久化，最终一致，fail-closed）+ VDF 预留位（§6，恒 null，算法不定型）+ 时序自洽（§7，无死锁）+ CC↔TW 切换点与 T3 互引。**未会签·单边定稿无效**。 |
| v0.2 | 2026-08-08 | P-FLOOD 补 **KNET-CC-010 节点 PM 附条件会签两条件履约**（2026-08-08 23:45 附条件通过）：**条件① N 下限** —— §4.2 补「N≥7（odd quorum 奇数仲裁集），共识需 ≥⌈2N/3⌉ 节点一致（N=7 时 ≥5）；N<7 时 fail-closed 不启动开关守安全」；§3.2 多点交叉核验同步对齐 N≥7 + ≥⌈2N/3⌉；§7.1 时序图补 N≥7 判定分支。**条件② fail-closed 收敛窗口** —— 新增 §4.4「T=3 周期告警不强制 OFF，计数继续等待节点上线，超 T 持续告警开关维持当前态」；§7.2 无死锁保证对齐收敛窗口。§8 自测门禁补 T8-10（N 下限）/ T8-11（收敛窗口）；§9 待定稿项更新两项状态为「条件履约」。**T9 签名覆盖引用维持不变，不重定义；VDF 算法参数仍归 P-ARCH 不定型（不越界）**。待 X-QA 回归 + 节点 PM 联合关闭。 |

---

*本文件为 T8 引导期计数与共识时间戳草案 v0.2。强制会签项，节点 PM 已附条件会签通过（KNET-CC-010，2026-08-08 23:45），本 v0.2 为两条件（N≥7 下限 + T=3 收敛窗口）履约，待 X-QA 回归 + 节点 PM 联合关闭后方可标「定稿」。count 包 wire 引用 T1 草案、签名覆盖引用 T9 草案，均不重定义。VDF 算法参数不定型，归 P-ARCH。*
