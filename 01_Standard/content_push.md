# KirinNet 定向内容推送与链接卡片协议（Content Push & Link Card）

> **Version:** 1.1（草案）
> **Date:** 2026-08-28（v1.1 修订；v1.0 基线 2026-08-24）
> **Status:** **已会签定稿（KNET-CC-012，2026-08-24 节点 PM 会签附条件通过·随单条件已履约·已合并 master·联合关闭）**；v1.1 增补（§11 订阅与裂变分发）为 **KNET-CC-017 草案·待会签，未会签单边定稿无效**
> **Scope:** 定向推送语义（T1 `publish` 包的直达变体）、链接卡片格式、展示节点接收/校验/存储/展示/跳转行为、抗屏蔽（opt-out）语义、节点广告位友情展现、统一端点契约、节点仓 12 篇公共索引（indexed_content）语义调整映射、订阅与裂变分发（订阅列表/受限裂变 N 跳/每关注对象转发开关/限频重定位——KNET-CC-017 草案 §11）
> **关联编号:** KNET-CC-012（**节点 PM 会签附条件通过 2026-08-24 22:30·条件履约·合并 master·联合关闭**）/ KNET-CC-017（分发模式扩展，**草案·待会签**；依据 §COM-01 2026-08-28 甲方三项裁定）/ 需求变更·爬虫下线·内容广告模型重构（`docs\协作记录.md` §COM-01，2026-08-09 甲方权威口径）/ T1（`publish` 包承载与信封，**引用不重定义**，KNET-CC-007 会签通过；§5.3 v0.2 签发前缀随 KNET-CC-015 会签关闭）/ T9（签名覆盖范围，**引用不重定义**，`DECISIONS.md` §9.3 + `did-dns-protocol.md` §3.5，KNET-CC-011 会签通过并关闭）/ T4（creator_domain 端点解析 direct/relay，**引用不重定义**）/ T3（好友图 `contacts` 与 `-127` 黑名单，**引用不重定义**，KNET-CC-008 会签通过）/ did-dns §2（did:dns 三记录，**引用不重定义**）/ 014（`cq_forward_per_min` 转发反滥用默认值先例，引用）/ 节点仓 10 篇 FR4（内容推送）/ 12 篇（公共索引）/ FR11（入站限频口径）
> **引用口径:** 被引文件（flood_protocol.md / did-dns-protocol.md / DECISIONS.md §9.3）文件头已随 KNET-CC-012 履约重戳为已会签定稿（CC-007/CC-011，2026-08-24）；本文件引用其 master 版本，**不重定义**其任何字段、算法与覆盖范围。
> **注:** v1.1 修订（§11 订阅与裂变分发）为 KNET-CC-017 草案·待会签，未会签单边定稿无效；版本行随本次修订对齐 1.x 序列（v1.0 = KNET-CC-012 定稿基线——此前文件头沿用草案号 0.1 未随定稿重排，版本演进见 §16 变更记录）。

---

## 0. 草案状态与依据

| 项 | 说明 |
|---|---|
| 草案定位 | v0.1 定义：① 定向推送 = T1 `publish` 包的**直达变体**（点对点，ttl=1，不走好友链泛洪）；② **链接卡片**（link card）格式——推送的是链接而非内容本体；③ 展示节点极轻行为契约（接收→校验 fail-closed→存卡片→展示→跳转回内容节点）；④ 抗屏蔽 opt-out 语义；⑤ 节点广告位友情展现（波 1 免费互换，付费留接口不定型）；⑥ 统一端点口径（`POST /api/v2/push/card`）；⑦ 节点仓 12 篇 indexed_content 语义调整映射建议 |
| v1.1 增补（草案） | KNET-CC-017（依据 §COM-01 2026-08-28 甲方三项裁定）：§11 订阅与裂变分发——双轨制（好友 T3 既有 / 订阅单向免同意可屏蔽）、追随者默认接收、受限裂变 N 跳（默认 2 提案·会签钉死）、屏蔽压制放大（-127/push_blacklist 复用）、每关注对象转发开关（三档：屏蔽/开关关/默认开）、限频重定位（反滥用闸）+ `pub_forward_per_min` 提案；卡片 11 字段/两层签名/§5 管线/§7 三原则/§8 广告位零改动。**未会签·单边定稿无效** |
| 需求依据 | 甲方权威口径（§COM-01 2026-08-09）：① 爬虫探索（节点仓 06 篇）下线；② 内容节点定向推送**链接**到展示节点（非内容本体）；③ 展示节点极轻（只存链接卡片，点击新开页面跳回内容节点）；④ 广告留在内容节点（展示节点不掺和）；⑤ 抗屏蔽（内容节点被部分节点屏蔽不影响其内容经其他节点展示）；⑥ 节点自身可有广告位（接其他节点投放或商业链接）；⑦ 节点间广告波 1 只做**友情展现**（免费互换），付费结算后定 |
| 反提炼对象 | 节点仓 `需求文档\10_社交与推送.md` FR4（push.js 推送现状：DoH 解析目标 + /health 探活 + POST metadata）+ `需求文档\12_公共索引.md`（indexed_content 薄索引、push_blacklist、已知缺陷 7.1 无签名校验 / 7.4 端点契约不符）+ 节点仓现状（2026-08-09 废弃处置 commit 0698512：explore.js/indexer.js/indexed_content 已下线删除，push.js 保留） |
| 会签记录 | 本文件属**跨仓端点/推送语义契约**，已经节点 PM 会签（**KNET-CC-012，2026-08-24 22:30 附条件通过·随单条件已履约（409 统一/invalid_slot 补句/被引文件头重戳）·已合并 master·联合关闭**）；协作记录 `docs\协作记录.md` §KNET-CC-012 为权威状态源。**v1.1 增补（§11 订阅与裂变分发 + 既有节对齐）为 KNET-CC-017 草案·待会签（未会签单边定稿无效），待 X-CC 立单流转，状态以 §KNET-CC 台账为准** |
| 不越界 | T1 信封结构与包类型枚举归 T1（本文件仅定义 `publish` 直达语义叠加，T1 §3 已明文「publish 语义归内容发布域，T1 仅承载」——本文件即该语义主笔）；签名算法/挑战码/覆盖范围归 T9（卡片级签名的**规范化规则沿用** T9 §9.3.2，覆盖对象为本文件新定义字段，属本文件契约不属 T9 修订）；creator_domain 端点解析（SRV/多候选/direct→relay 回退）归 T4/spec_v1；中继隧道 wire 编码归波 1 T7；付费投放结算语义**不定型**（付费后定）；商业广告位（08/09 篇）节点侧逻辑调整归节点 PM |

---

## 1. 设计动机与原则（需求变更后的内容分发模型）

### 1.1 模型总述

```
┌─────────────────┐  ① 定向推送链接卡片（HTTP POST，非泛洪扩散）   ┌─────────────────┐
│   内容节点       │ ───────────────────────────────────────────▶ │   展示节点（极轻） │
│  alice.example  │                                               │  bob.example    │
│                 │  ② 只存卡片：title/summary/缩略图链接/签名       │  不存内容本体     │
│  内容本体+内广告 │                                               │  不掺和广告      │
│  数据主权在此     │ ◀───────────────────────────────────────────  │                 │
└─────────────────┘  ③ 点击卡片 → 新开页面跳回内容节点（落地页广告   └─────────────────┘
                        在内容节点自己的内容里展示）
```

**本质转变**（§COM-01 2026-08-09）：从「爬虫主动爬取 + 本地索引」（06 篇，已下线）改为「定向推送链接 + 展示节点轻卡片 + 跳转回内容节点」。**数据主权在内容节点**：内容、内嵌广告、访问统计全部留在创作者手里；展示节点只持有可验签的轻卡片。

> **v1.1 增补（KNET-CC-017 草案·待会签）:** 在定向直达之外新增**订阅裂变分发**（§11）——发布者发布即默认触达粉丝，沿订阅图受限转发 N 跳（默认 2 提案）。**两种模式并存、互不废除**：定向直达（现行，手动单推）+ 订阅裂变（新增，发布默认通道）；上图描绘定向直达形态，其语义不变。

### 1.2 协议原则（甲方口径逐条落协议语义）

| # | 甲方口径 | 协议落点 |
|---|---|---|
| P1 | 推送的是**链接**，非内容本体 | §3 链接卡片：payload 仅含 title/summary/thumbnail_url/content_key 等定位与摘要字段，无内容体 |
| P2 | 展示节点**极轻** | §5.4：展示节点禁止拉取/缓存/解析内容本体；卡片 JSON ≤2 KiB；缩略图可选直链加载或禁用 |
| P3 | 点击**新开页面跳回内容节点** | §6：跳转 URL 由 `creator_domain + content_key` 构造；新开标签页（`noopener`）；禁止 iframe/代理/包装 |
| P4 | **广告留在内容节点** | §6.3：展示节点 MUST NOT 注入、包装或在跳转链路掺和广告；落地页内广告由内容节点自主呈现 |
| P5 | **抗屏蔽** | §7：屏蔽 = 展示节点本地 opt-out（拒收 + 本地下架），无全网删除义务，不影响其他节点展示 |
| P6 | 节点自身可有**广告位** | §8：`ad_slot` 卡片（复用链接卡片结构 + `slot_id`），链接来自其他节点投放或商业链接 |
| P7 | 节点间广告波 1 只做**友情展现** | §8.3：免费互换投放；`price_hint` 恒 `null` 留接口字段，付费语义**不定型**（付费结算后定） |
| P8 | 爬虫探索下线，探索发现改走泛洪 | 定向推送与 T1 泛洪公告互补（§2）；本文件不涉及探索发现机制（06 篇已下线，不引用） |

### 1.3 与去中心化纪律的关系

- 定向推送是**点对点 opt-in 关系**（内容节点 → 明确选定的展示节点），不是广播、不依赖中心索引/爬虫/注册系统（本条限定向直达形态；订阅裂变〔§11，草案〕沿订阅图逐跳背书扩散，同样不依赖中心索引/爬虫/注册——同守去中心化纪律）。
- 防滥用是**接收方本地行为**（黑名单 opt-out + 入站限频），无中心化限流网关——与 T1 §8「配额与限频是节点本地行为」同一纪律。
- 身份与防伪依赖 **did:dns 三记录 + Ed25519**（did-dns §2，TXT 全量 `did:dns:`，统一 Ed25519 红线），身份记录解析**强制 DoH/DoT，禁明文 53**（架构红线）。

---

## 2. 与 T1 泛洪协议的关系（`publish` 的直达变体）

### 2.1 归属声明

T1（`flood_protocol.md`）§3 已明文：`publish`（内容发布泛洪）的**语义由内容发布域定义，T1 仅定义信封承载位**。本文件即该内容发布域的语义主笔：定义 `publish` 的投递形态（`delivery`）——v1.0 两形态（直达/泛洪）+ v1.1 新增订阅裂变（§11，KNET-CC-017 草案·待会签），三形态并存。

### 2.2 投递形态（直达 / 订阅裂变 / 泛洪）

| 形态 | `payload.delivery` | 通道 | ttl | forward_chain | 转发义务 | 泛洪配额 | 状态 |
|---|---|---|---|---|---|---|---|
| **直达（direct）** | `"direct"` | 点对点 HTTPS `POST /api/v2/push/card`（§9） | **恒 1** | **恒 `[src_domain]`**（单元素） | **无**（接收方 MUST NOT 转发） | **不计**（非好友链扩散） | **波 1 主形态**（本文件定稿依据） |
| **订阅裂变（fanout）** | `"fanout"` | 订阅图逐跳接力 HTTPS `POST /api/v2/push/card`（§11.5，发布→粉丝扇出→逐跳转发） | **N**（默认 2 提案·会签钉死；上界 6 = T1 §4.1 `publish` 上限） | 链式追加（T1 §5.1；验签锚定签发前缀 T1 §5.3） | **按订阅图与转发开关**（§11.5 逐跳三查：查开关→查屏蔽→转发） | **计入每日 3 次**（T1 §3/§8 既有——泛洪类扩散） | **KNET-CC-017 草案·待会签（§11；会签通过前不得启用，fail-closed）——发布默认通道，不废除直达** |
| 泛洪（flood） | `"flood"` | 好友链受限泛洪（T1 §3 `publish` 原义） | ≤6（T1 §4.1） | 链式追加（T1 §5） | 有（T1 转发规则） | **计入每日 3 次**（T1 §8） | 节点泛洪引擎就绪后启用（fail-closed，引擎未实现前不得启用） |

> **并存口径（§COM-01 2026-08-28）:** 定向直达（现行·手动单推）与订阅裂变（新增·发布默认通道）**两种模式并存、互不废除**；订阅裂变走订阅图（§11.1 双轨制），不占用好友链泛洪（`flood` 形态排期不变）。

### 2.3 直达变体的语义叠加（不改 T1 任何字段定义）

本文件对 T1 信封（§2 公共字段）**零改动、零重定义**，仅施加定向约束：

1. `type` 恒为 `"publish"`（复用 T1 §3 包类型枚举，不新增类型）；
2. `ttl` 恒为 `1`：接收方校验 `ttl ≠ 1` → 拒收（fail-closed，防直达包被误作泛洪包扩散）；
3. `forward_chain` 恒为 `[src_domain]`（单元素）：链长 ≠1 → 拒收；
4. 接收方 **MUST NOT 转发**直达包（无扩散义务；转发即违反定向语义）；
5. 直达推送**不计入** T1 §8 每日 3 次泛洪配额（该配额约束好友链扩散；直达是点对点请求，防滥用由接收方本地限频/黑名单承担，§5.2/§7）；
6. `nonce`/`query_id` 沿用 T1 §2.1 语义（防重放去重），挑战码结构**引用 T9 §9.3.1**；
7. `signature` 覆盖范围**引用 T9 §9.3.2**（挑战码 + 双方域名 + `forward_chain`；第 5 项 `trust_weight` 不适用——本包不携带权重字段），算法/规范化归 T9，本文件不重定义。

> **依据对照:** 节点仓 10 篇 FR4 的现行实现（push.js：DoH 解析目标域名 + `/health` 探活 + POST metadata 到目标索引）即直达形态的雏形；本文件将其契约化为 `delivery:"direct"`。

---

## 3. 链接卡片格式（link card）

### 3.1 结构

```
{
  "v": 1,
  "card_type": "content",
  "title": "<标题, UTF-8, 1~120 字符>",
  "summary": "<摘要, UTF-8, 0~300 字符>",
  "thumbnail_url": "<HTTPS 缩略图直链 | null>",
  "creator_domain": "<内容节点域名, ASCII>",
  "content_key": "<内容定位键, ASCII>",
  "content_type": "<article | product | post | video>",
  "iat": <卡片签发时间, Unix 秒>,
  "exp": <卡片过期时间, Unix 秒>,
  "creator_fp": "<creator_domain 的 did:dns 公钥指纹, 16 字符>",
  "creator_sig": "<Ed25519 卡片签名, Base64URL>"
}
```

> 风格对齐 did-dns §2 三记录（简洁、单条轻量、自包含可验）。卡片 JSON 序列化后 **MUST ≤ 2 KiB**（请求体整体 ≤ 8 KiB，对齐节点侧 F01 解密端点 body 上限口径）。

### 3.2 字段定义

| 字段 | 类型 | 必选 | 约束 | 含义 |
|----|------|------|------|------|
| `v` | int | 是 | 固定 `1` | 卡片格式版本 |
| `card_type` | enum | 是 | `content` / `ad_slot`（§8） | 内容卡片 / 广告位卡片 |
| `title` | str | 是 | UTF-8，1~120 字符 | 卡片标题（展示节点列表/搜索主字段） |
| `summary` | str | 否 | UTF-8，0~300 字符 | 摘要（展示节点可直接呈现，不抓取内容） |
| `thumbnail_url` | str/null | 否 | HTTPS URL，≤512 字符；`null` = 无缩略图 | 缩略图**直链**（存链接不存图）；展示节点可策略性禁用加载（§5.4） |
| `creator_domain` | str | 是 | ASCII 域名，≤253 字符，与信封 `src_domain` **必须一致** | 内容节点域名（did:dns:v=1 身份锚点，跳转目标域） |
| `content_key` | str | 是 | ASCII `[A-Za-z0-9._~-]{1,128}`（RFC 3986 unreserved，禁 `/ ? # & %` 等保留字符——防跳转 URL 路径注入） | 内容定位键（内容节点内唯一，如内容 ID/slug） |
| `content_type` | enum | 是 | `article` / `product` / `post` / `video`（对齐节点仓 05/12 篇 content_type 基线；`ad_slot` 卡片恒为 `ad`，§8.2） | 内容类型（展示节点分类浏览依据） |
| `iat` | int | 是 | Unix 秒；接收时刻偏差 MUST ≤ ±5 分钟（对齐 did-dns §3.3 第 4 条新鲜度，防旧卡重放） | 卡片签发时间 |
| `exp` | int | 是 | Unix 秒；MUST `exp > iat`；建议 `exp − iat ≤ 90 天`（`ad_slot` 建议 ≤30 天），到期后展示节点本地下架（§5.5） | 卡片有效期（**卡片**有效期，非内容有效期——内容生命周期归内容节点） |
| `creator_fp` | str | 是 | 16 字符 = `Base64URL(SHA-256(公钥)[0:12])`（**引用 did-dns §2.1 fp 定义，不重定义**） | 绑定 creator_domain 的 did:dns:v=1 身份声明指纹；接收方验签时比对，防旧钥/他钥冒名 |
| `creator_sig` | str | 是 | Ed25519 签名 64 字节 Base64URL（约 86 字符），生成规则见 §4.3 | 卡片级签名：证明卡片确由 creator_domain 持钥者签发（防伪造卡片）；卡片存储后可独立重验 |

### 3.3 卡片定位与幂等键

- 全网唯一定位：`(creator_domain, content_key)` 二元组。
- 展示节点以该二元组作 **upsert 幂等键**：同键重推 = 更新卡片（标题/摘要/缩略图/新 iat/exp/新签名），非重复收录（§9.3 响应 `updated:true`）。
- 删除/下线语义：内容节点重推同键卡片即覆盖；协议**不提供跨节点撤回原语**（抗屏蔽语义，§7）。

---

## 4. 定向推送包（wire 格式）

### 4.1 整体结构

```
POST https://<展示节点域名>/api/v2/push/card
Content-Type: application/json

{
  "v": 1,                                    ← T1 信封（引用 §2，不重定义）
  "type": "publish",
  "src_domain": "alice.example",
  "ttl": 1,
  "query_id": "<发送方生成随机标识, ASCII ≤64>",
  "nonce": "<挑战码 nonce:timestamp:hmac, 引用 T9 §9.3.1>",
  "forward_chain": ["alice.example"],
  "signature": "<信封签名, 覆盖引用 T9 §9.3.2>",
  "payload": {
    "v": 1,
    "delivery": "direct",
    "target_domain": "bob.example",
    "card": { ...链接卡片, §3... }
  }
}
```

- 信封字段（`v`/`type`/`src_domain`/`ttl`/`query_id`/`nonce`/`forward_chain`/`signature`）**全部引用 T1 §2.1 定义**，本文件不重定义；定向约束见 §2.3。
- `payload.delivery`/`payload.target_domain` 为本文件新增（payload 归内容发布域主笔，T1 仅承载）。
- `payload.target_domain` = 接收方（展示节点）域名：① 供接收方核对 HTTP 目标与签名一致；② 纳入卡片级签名输入（§4.3），防卡片跨节点重放。

### 4.2 两层签名的分工

| 层 | 字段 | 签名者 | 作用 | 覆盖范围 |
|---|---|---|---|---|
| 传输级（信封） | `signature` | 发送方节点（Ed25519 节点身份钥） | 防传输重放（nonce 一次性）+ 绑定双方域名 + 防转发链篡改 | **引用 T9 §9.3.2**（挑战码 / 校验方域名=target / 受验方域名=src / forward_chain；第 5 项权重不适用） |
| 卡片级 | `card.creator_sig` | 内容创作者（creator_domain 持钥者） | **防伪造卡片** + 卡片离线可验（存储后与传输会话解耦，任何持 did:dns:pk 者可独立重验） | 本文件 §4.3 定义（新契约，非 T9 修订） |

> 卡片级签名独立存在的理由：卡片被展示节点**长期存储**并对外呈现，其可验证性必须自包含（不依赖推送时刻的传输会话）；且广告位互换（§8）与未来的代理投递形态下，信封发送方与卡片创作者可能分离，两层签名权责清晰。

### 4.3 卡片级签名（creator_sig）生成与验证

**签名输入**（固定顺序、`\n` 分隔；JSON 规范化**沿用 T9 §9.3.2 规则**——JCS（RFC 8785）后签名，禁止字段重排/省略后签名）：

```
sign_input = JCS(card 除 creator_sig 外的全部字段) + "\n" + payload.target_domain
```

- 签名算法：Ed25519（64 字节，Base64URL）——统一 Ed25519 红线（DECISIONS §9.2.3）。
- `target_domain` 纳入输入：卡片绑定接收节点，**防跨节点卡片重放**（为节点 B 签的卡片不能被转投节点 C；内容节点向 N 个展示节点推送时逐个签发，成本可忽略）。
- 验证（接收方/任何重验者）：
  1. DoH/DoT 解析 `creator_domain` TXT → `did:dns:v=1`（校验 `iat` ±5min、`fp ∉ black`）→ `did:dns:pk`（`kty=ed25519`）→ `did:dns:black`（**引用 did-dns §2/§3 记录与校验规则，不重定义**）；
  2. 比对 `card.creator_fp == did:dns:v=1 的 fp`；
  3. 以 `did:dns:pk` 公钥验 `creator_sig`。
- DNSSEC 可用时应校验（did-dns §2.1 建议）；无 DNSSEC 时以 DoH/DoT 加密通道 + 指纹链兜底（与 did-dns §5 威胁模型一致）。

---

## 5. 展示节点行为（接收 → 校验 → 存储 → 展示）

### 5.1 接收流程总览

```
① 传输与信封校验（§5.2）
② 卡片校验（§5.3，任一步失败 → fail-closed 拒收，不静默收录）
③ 黑名单 opt-out 检查（§7）
④ 存储：卡片原文 + 验签元数据（§5.4，极轻）
⑤ 展示：列表/搜索/滑动仅基于卡片字段（§5.5）
⑥ 点击：新开页面跳转内容节点（§6）
```

### 5.2 传输与信封校验

| # | 校验项 | 失败处置 |
|---|---|---|
| 1 | HTTPS（强制；身份记录解析 DoH/DoT，禁明文 53） | 拒绝（传输层红线） |
| 2 | 信封结构完整（T1 §2 字段齐全、类型正确）、`v=1`、`type="publish"` | 400 `invalid_envelope` |
| 3 | 形态约束：`delivery="direct"` → `ttl == 1` 且 `forward_chain == [src_domain]`（§2.3）；`delivery="fanout"` → `1 ≤ ttl ≤ N`、`forward_chain[0] == src_domain`（T1 §5.3-4）且链合法（T1 §5.2，§11.4） | 400 `not_direct_form` / `not_fanout_form`（fanout 形态，KNET-CC-017 草案） |
| 4 | 目标绑定：`delivery="direct"` → `payload.target_domain == 本节点域名`；`delivery="fanout"` → `payload.target_domain == "*"`（通配，§11.4）且本节点订阅了 `forward_chain` 链尾直接上游（订阅图准入，§11.5） | 400 `target_mismatch` / 403 `not_subscribed`（fanout 形态，KNET-CC-017 草案） |
| 5 | `query_id + nonce` 去重表未命中（T1 §5.2 防重放；命中即重放包） | 409 `replayed`（节点 PM 会签裁定**统一 409**，幂等重放，§9.3） |
| 6 | 信封 `signature` 验签（覆盖引用 T9 §9.3.2；公钥 = DoH/DoT 解析 `src_domain` 的 `did:dns:pk`） | 400 `bad_signature` |
| 7 | 入站限频（**反滥用闸——仅防恶意攻击，非行为约束**，§11.6 重定位）：每 `src_domain` ≤30 次/分（默认值·节点本地可调；fanout 计数键 = 信封 `src_domain`） | 429 `rate_limited` |

### 5.3 卡片校验（fail-closed）

| # | 校验项 | 失败处置 |
|---|---|---|
| 1 | 字段完整/类型/长度（§3.2 约束逐项：title≤120、summary≤300、content_key 字符集、卡片 ≤2 KiB 等） | 400 `invalid_card` |
| 2 | `card.creator_domain == 信封 src_domain`（直达形态下推送方即创作者；本版不支持代理投递。fanout 形态下任一跳同样成立——转发者仅追加 `forward_chain`、不改写 `src_domain`〔T1 §5.1〕，转发≠代理投递，§11.4） | 400 `creator_mismatch` |
| 3 | `iat` 接收时刻偏差 ≤±5 分钟；`exp > iat`；未过期 | 400 `stale_card` / `expired_card` |
| 4 | `content_type` 枚举合法（`ad_slot` 卡片走 §8 附加校验） | 400 `invalid_card` |
| 5 | did:dns 三记录校验（DoH/DoT 解析 creator_domain：v=1 新鲜度 ±5min、pk kty=ed25519、fp∉black） | 400 `creator_dns_invalid` |
| 6 | `creator_fp` == v=1 记录的 `fp`；`creator_sig` 验签（§4.3） | 400 `bad_creator_sig` |

> **fail-closed 红线：** 任一校验失败即拒绝收录（4xx 明确响应）；**禁止**「先收录后补验」或「验不过仍暂存」。（修复节点仓 12 篇已知缺陷 7.1——原 publish 无签名校验只存不验，可伪造 creator_domain 灌数据。）

### 5.4 存储（极轻清单）

| MUST（禁止项） | 说明 |
|---|---|
| **不拉取内容本体** | 仅存卡片；不得回源抓取 `content_key` 指向的页面/文件 |
| **不缓存缩略图文件** | `thumbnail_url` 存直链；加载与否由展示节点本地策略（隐私/流量），禁改写为本地代理图床 |
| **不解析/索引内容文本** | 搜索仅基于卡片 title/summary（对齐 12 篇「不做搜索排名算法」边界） |
| **不修改卡片语义字段** | title/summary/content_key/跳转 URL 原样呈现（本地仅 `is_listed` 上下架开关） |

存储建议（实现细节归节点 PM，§10 给出表列映射）：存卡片 JSON 原文（含 `creator_sig`，自包含可重验）+ 验签元数据（`creator_fp`/接收时间）+ 本地展示开关 + 点击计数。

### 5.5 展示与生命周期

- 列表/搜索/滑动/趋势：仅基于卡片字段（title/summary/content_type/iat/点击数）——12 篇原有检索/滑动体验对卡片数据源平移（数据源由「爬取/被动聚合」改为「定向推送收录」）。
- 点击：§6 跳转；点击计数 +1（本地点击数，非内容访问量——内容访问统计在内容节点）。
- 过期：`exp` 到期 → 本地下架（`is_listed=false`），已存记录保留（创作者重推同键新卡即恢复展示）。
- 黑名单：§7 opt-out。

---

## 6. 跳转与广告边界

### 6.1 跳转 URL 构造（creator_domain + content_key）

```
jump_url = "https://" + creator_domain + "/view/" + content_key
```

- 该规则是**跨仓契约**：内容节点（creator_domain 侧）MUST 提供 `GET /view/<content_key>` 公开内容页（未认证可读、移动端可用）；`content_key` 字符约束（§3.2）保证构造无歧义。
- 波 1 过渡：内容节点暂无独立 `/view/` 页时，可暂以其 SPA 内路由承接（如 `/app#/view/<content_key>`），但**定稿口径以 `/view/<content_key>` 为准**（KNET-CC-012 会签时节点 PM 确认落地路径）。
- 展示节点 MUST 新开页面（`target="_blank"` + `rel="noopener noreferrer"`）。
- creator_domain 端点解析（SRV 发现/多候选/direct→relay 回退/NAT 后仅 relay）**引用 T4 与 spec_v1 §3.1**，本文件不重定义；浏览器直跳场景按标准 DNS+HTTPS 即可。

### 6.2 点击行为约束

| MUST NOT | 理由 |
|---|---|
| 不得 iframe/反代/包装内容页 | 内容与广告主权在内容节点（P4） |
| 不得在跳转前插中间页/倒计时/自家广告 | 同上 |
| 不得篡改 jump_url（加参数跟踪除外，须本地策略明示） | 卡片语义原样呈现 |

### 6.3 广告边界（广告留在内容节点）

- 内容内广告：在内容节点落地页自己的内容里展示，展示节点**不掺和**（不注入、不分成、不感知）。
- 展示节点自身的广告位（站内广告位卡片）是**独立机制**（§8），与所展示的内容卡片互不掺杂：广告位是节点自己的版面经营，内容卡片是替其他节点呈现的链接。

---

## 7. 抗屏蔽语义（屏蔽 = 接收方 opt-out）

### 7.1 三原则（MUST）

| # | 原则 | 协议语义 |
|---|---|---|
| 1 | **屏蔽是接收方本地 opt-out** | 展示节点将 `creator_domain` 加入本地 `push_blacklist` → ① 后续该域名推送 403 拒收；② 已收录卡片本地下架（`is_listed=false`）。仅此而已 |
| 2 | **不产生全网删除义务** | 屏蔽节点的处置对其他节点**无任何效力**；协议不提供跨节点删除/撤回原语（§3.3）；无全局索引可被要求清理 |
| 3 | **不阻止其他节点展示** | 内容在创作者手里（内容节点），卡片经其他展示节点继续呈现；内容节点可同时向任意多个展示节点定向推送，被部分屏蔽不影响其余链路 |

### 7.2 与既有黑名单机制的关系（不合并、不重定义）

| 机制 | 层 | 归属 | 效力范围 |
|---|---|---|---|
| `push_blacklist`（12 篇，表已保留） | 展示/收录层 | 本文件 §7 | 本节点拒收+下架该创作者卡片 |
| T3 `-127` 黑名单（IM/解析层） | 好友链全链路 | **引用 T3 §6，不重定义** | 本节点不转发其泛洪包、不响应其查询 |

两层独立生效、互不替代（站长可只用其一）；合并与否留节点侧实现策略，本文件不定。

---

## 8. 节点广告位（波 1：友情展现）

### 8.1 模型

节点自身版面可设广告位（slot）：**链接来自其他节点投放**（友情互换，免费）或商业链接（08/09 篇商业线，节点侧自理，不在本协议范围）。波 1 节点间广告只做**友情展现**——免费互换投放，无结算。

### 8.2 广告位卡片（ad_slot，复用链接卡片结构 + slot 标识）

```
{
  "v": 1,
  "card_type": "ad_slot",
  "slot_id": "<目标节点广告位标识, ASCII [a-z0-9-]{1,32}, 如 home-top / sidebar-1>",
  "title": "<广告标题, 1~120 字符>",
  "summary": "<广告摘要, 0~300 字符>",
  "thumbnail_url": "<素材直链 | null>",
  "creator_domain": "<投放方（内容）节点域名>",
  "content_key": "<落地页定位键>",
  "content_type": "ad",
  "price_hint": null,
  "iat": ..., "exp": ...,
  "creator_fp": "...",
  "creator_sig": "..."
}
```

- 与内容卡片的差异仅 3 处：`card_type="ad_slot"`、新增 `slot_id`（投放目标位标识，由展示节点对外公布）、`price_hint`（§8.3）。
- `content_type` 恒 `"ad"`（落地页具体形态不影响广告位展示）。
- 校验/签名/存储/跳转**完全复用** §4~§6 契约（同一验签链、同一跳转构造）；`exp − iat` 建议 ≤30 天（友情位短周期互换）。
- `ad_slot` **附加校验**（`slot_id` 格式 `[a-z0-9-]{1,32}`、`content_type` 恒 `"ad"`、`price_hint` 恒 `null` 等 §8 约束）失败 → **400 `invalid_slot`**（错误码与 §9.3 枚举对应；区别于 §5.3 通用卡片校验失败的 `invalid_card`）。
- 广告点击后落地页及其广告归投放方节点（同 §6.3 边界）。

### 8.3 付费投放：留接口不定型（守「付费结算后定」口径）

- `price_hint` 字段**恒 `null`**（MUST）：为付费投放预留的接口字段，波 1 无语义。
- 发送方 MUST 置 `null`；接收方 MUST 忽略其值（出现非 `null` 值不构成任何报价/要约/结算依据，也不得据此拒收——字段值被无条件忽略）。
- 付费投放的计费/结算/对账语义**待付费体系裁决后另立草案**（届时经 KNET-CC 修订本文件），本文件不预设任何付费模型。

### 8.4 友情互换投放端点

- 投放走 §9.2 端点 `POST /api/v2/push/adslot`（与内容卡片同族、分开端点便于节点分别开关）。
- 互换是**双边自愿**：A 节点向 B 投放不产生 B 向 A 投放的义务；接收方对 ad_slot 卡片的拒收/下架自由度同 §7 opt-out。
- 展示节点对外公布可用 `slot_id` 清单的方式（端点/页面）由节点侧自理，本文件不定。

---

## 9. 端点契约（统一口径）

### 9.1 现状勘误（为何必须统一）

| 旧路径 | 现状 | 结论 |
|---|---|---|
| `POST /api/v1/publish`（push.js:80/191 现推路径） | **从未存在**（节点仓已知 bug，10 篇缺陷 7.4：push.js 目标端点与本地实现不符） | 废弃 |
| `POST /api/indexer/publish`（原接收端，indexer.js） | **已下线删除**（2026-08-09 废弃处置 commit 0698512：indexer.js 被动聚合层与 indexed_content/ingestion_log 表一并移除） | 废弃 |

两条旧路径均不可用；本节给出**统一新口径**，作为 KNET-CC-012 会签依据（端点路径变更属跨仓契约）。

### 9.2 端点定义

| 端点 | 用途 | 认证 | 实现 |
|---|---|---|---|
| `POST /api/v2/push/card` | 接收**内容链接卡片**推送（请求体 = §4.1 定向推送包；`delivery="fanout"` 裂变包分流校验 §11.4/§11.5——KNET-CC-017 草案） | 无用户级认证（身份=两层签名，§4.2；本地黑名单/限频防滥用） | 波 1 **必选** |
| `POST /api/v2/push/adslot` | 接收**广告位卡片**友情投放（请求体 = §4.1，card 为 §8.2 ad_slot 卡） | 同上 | 波 1 可选（节点未开广告位可 404） |
| `POST /api/v2/push/subscribe` / `DELETE /api/v2/push/subscribe` | **订阅管理**（订阅/取消，免同意登记入粉丝名册，§11.2/§11.9） | 无用户级认证（订阅方域名 did:dns 校验，§11.9；防滥用归节点本地策略） | **KNET-CC-017 草案·待会签（会签前不得启用，fail-closed）** |

- 命名进入 `/api/v2/` 命名空间：与两条已废弃 v1/indexer 路径彻底切割，避免旧路径兼容负担。
- 发送方流程（push.js 演进依据，10 篇 FR4 契约化）：DoH 解析目标域名（强制加密 DNS）→ `GET /health` 探活 → 逐目标构造 §4.1 推送包（`target_domain` 逐个签发卡片，§4.3）→ POST。（v1.1 草案：fanout 形态下发布方对粉丝名册逐个 POST 同一信封/卡片，`target_domain` 恒 `"*"` 一次签发——§11.5。）

### 9.3 响应契约

| 状态码 | 场景 | 响应体 |
|---|---|---|
| 200 | 收录成功 / 幂等重推更新 | `{"status":"accepted","card_id":<本地ID>,"updated":<bool>}`（`(creator_domain,content_key)` 命中已有记录 → upsert，`updated:true`） |
| 400 | 校验失败（§5.2/§5.3 各项，fail-closed 明确拒因） | `{"error":"<invalid_envelope|not_direct_form|target_mismatch|bad_signature|invalid_card|creator_mismatch|stale_card|expired_card|creator_dns_invalid|bad_creator_sig|invalid_slot|not_fanout_form（KNET-CC-017 草案）>","detail":"..."}` |
| 409 | 防重放命中（§5.2 第 5 项：`query_id + nonce` 已见于去重表，节点 PM 会签裁定**统一 409**） | `{"error":"replayed"}`（幂等重放：重放包不重复收录、不触发 upsert 更新；与 200 `updated:true` 的同键新信封重推区分） |
| 403 | 黑名单 opt-out（§7）/ 裂变订阅图准入失败（§11.5，KNET-CC-017 草案） | `{"error":"blacklisted"}` / `{"error":"not_subscribed"}`（fanout 形态） |
| 404 | 端点未启用（展示功能关闭 / 未开广告位） | `{"error":"not_enabled"}` |
| 429 | 入站限频（§5.2 第 7 项） | `{"error":"rate_limited","retry_after_ms":<ms>}` |
| 503 | 节点未就绪（未初始化等） | `{"error":"not_ready"}` |

---

## 10. 与节点仓 12 篇公共索引的映射（indexed_content 语义调整建议）

### 10.1 语义调整

| 项 | 调整前（12 篇 v1.0） | 调整后（本文件） |
|---|---|---|
| 表语义 | 薄 metadata 索引（cid/description/storage_type 等内容元数据） | **链接卡片存储**（更轻：仅卡片字段+验签元数据，无内容元数据） |
| 数据来源 | 外部节点 POST metadata（无验签，缺陷 7.1）+ 爬取消费（06 篇，已下线） | **接收定向推送**（§5 fail-closed 全量校验后收录） |
| 消费方式 | 全文搜索/滑动/趋势 + 站内点击 | 不变（列表/搜索/滑动/趋势对卡片平移，§5.5）+ 点击**跳出本站**（跳转回内容节点） |
| 广告展示 | `/api/indexer/ads` 联动 09 篇竞价 | 内容卡片与广告**解耦**（§6.3）；节点自身广告位独立走 §8 |

### 10.2 卡片字段 → 表列映射（重建建议，实现细节不强制）

> 现状说明：`indexed_content`/`ingestion_log` 已随 indexer.js 下线移除（commit 0698512，2026-08-09）；`push_blacklist` 表保留。展示节点重建收录存储时建议按下表（列名/类型供节点 PM 参考，非强制）：

| 卡片/协议字段 | 建议列 | 类型 | 说明 |
|---|---|---|---|
| `(creator_domain, content_key)` | 联合 UNIQUE 键 | TEXT×2 | 幂等 upsert 键（§3.3） |
| `card_type` | `card_type` | TEXT | content / ad_slot |
| `title` | `title` | TEXT NOT NULL | |
| `summary` | `summary` | TEXT | 可空 |
| `thumbnail_url` | `thumbnail_url` | TEXT | 可空（直链不存图） |
| `content_type` | `content_type` | TEXT | article/product/post/video/ad |
| `slot_id`（仅 ad_slot） | `slot_id` | TEXT | 可空 |
| `iat` / `exp` | `card_iat` / `card_exp` | BIGINT | 过期下架依据（§5.5） |
| `creator_fp` | `creator_fp` | TEXT(16) | 验签指纹（重验比对） |
| `creator_sig` + 卡片原文 | `card_raw` | TEXT | 卡片 JSON 原文（自包含可重验，§4.2） |
| — | `is_listed` | BOOLEAN DEFAULT TRUE | 本地展示开关（过期/隐藏/黑名单下架） |
| — | `received_at` | BIGINT | 收录时间（简化替代 ingestion_log；如需收录审计可自便保留 log 表，非协议要求） |
| — | `view_count` | BIGINT DEFAULT 0 | 点击计数（本地点击，12 篇口径保留；防刷归节点侧本地策略） |

`push_blacklist` 表（domain, reason）直接复用——§7 opt-out 的存储载体（12 篇原语义保留）。

---

## 11. 订阅与裂变分发（KNET-CC-017 草案·待会签）

> **草案状态:** 本节为 v1.1 增补（KNET-CC-017，需求依据 §COM-01 2026-08-28 甲方三项裁定：双轨制〔订阅+受限裂变+每关注对象转发开关〕/屏蔽压制放大/追随者默认接收/ad_slot 不变），**未会签单边定稿无效**；会签通过前节点侧不得启用 `fanout` 投递形态与 subscribe 端点（fail-closed）。本节为对既有条款的**零改动叠加**：卡片 11 字段（§3）、两层签名（§4）、§5 校验管线、§7 抗屏蔽三原则、§8 广告位定向互换均维持原文，与既有形态的差异点在本节内逐处明示。

### 11.1 定位与双轨制（两种分发关系并存）

| 轨 | 关系性质 | 载体 | 同意语义 | 屏蔽 |
|---|---|---|---|---|
| 好友（既有） | IM/信任，双向 | T3 §2.2 `contacts`（trust_weight 好友边）+ 好友链泛洪（§2.2 `flood` 形态，排期不变） | IM 语境既有（双向） | T3 `-127`（T3 §6） |
| **订阅（本节新增）** | **内容分发，单向追随** | 本节订阅列表（§11.2，节点本地轻量契约） | **免同意**——订阅无需被订阅方批准（被订阅方不可拒绝被订阅，只能屏蔽） | **可随时屏蔽**——复用 `push_blacklist`（§7）+ T3 `-127`（T3 §6），零新增机制 |

- 两轨并存互不替代：订阅不进入 T3 取值域（不新增权重语义）、不写 `contacts`；好友链 `flood` 形态排期不变（§2.2）。
- **两种推送模式并存（不废除既有模式）**：定向直达（§2，现行——手动单推、点对点 ttl=1）与**订阅裂变（本节，新增——发布侧默认通道：发布即默认触达粉丝**，§COM-01 2026-08-28「追随者默认接收」）。发布者可两者并用（裂变触达粉丝圈 + 直达精准单推）。

### 11.2 订阅列表（subscriptions，节点本地）

- **单向**：订阅是订阅方本地行为（登记「我订阅了谁」）；**免同意**——被订阅方无批准动作，粉丝名册由订阅动作登记形成（§11.9）。
- **可随时被屏蔽**：被订阅方对订阅者的压制复用既有两机制（不合并、不重定义，同 §7.2）：`push_blacklist`（创作者域，拒收+下架）/ T3 `-127`（节点域，不转发其包、不响应其查询，T3 §6.2）。
- **每条订阅一个转发开关**：`forward_enabled`（BOOLEAN，**默认 true**）——本节点对该发布者内容「收后是否放大」的本地决策位（三档语义见 §11.3）。开关挂在「本节点 → 直接上游（所订阅的发布者）」条目上，控制对该来源（其自发内容与其转发的背书内容）的放大。
- 表结构建议（非强制，实现归节点 PM）：`publisher_domain`（TEXT，UNIQUE）/ `subscribed_at`（BIGINT）/ `forward_enabled`（BOOLEAN DEFAULT TRUE）。
- **粉丝名册（followers）**：发布方本地登记的「谁订阅了我」——发起扇出与逐跳转发的目标清单即各自本地粉丝名册。节点仓既有 followers 机制（10 篇内容分发）可复用为载体（实现细节归节点 PM，本文件不重定义）。
- **追随者默认接收**：push 发布时订阅者（粉丝）默认收到——唯一不收的情形是屏蔽档（§11.3 第 1 档）。

### 11.3 三档语义（屏蔽 / 开关关 / 默认开）

| 档 | 机制（全部既有复用） | 收 | 转 | 语义 |
|---|---|---|---|---|
| **屏蔽** | `push_blacklist`（创作者域，§7）/ T3 `-127`（节点域，T1 §5.2·T3 §6.2） | **否**（403 拒收） | **否** | **不收不转**——屏蔽即压制放大（§COM-01 2026-08-28 口径：被屏蔽者的推送不被转发） |
| **开关关** | 订阅表 `forward_enabled=false`（§11.2） | **是**（本地收录展示） | **否** | **收而不转**——只自己看、不放大（比屏蔽细一档） |
| **默认开** | `forward_enabled=true`（默认值） | **是** | **是**（受 N 跳/去重/反滥用闸约束，§11.5~§11.7） | 正常裂变 |

- **开关语义 = 「转发即背书，放大权归转发者」**（甲方补充创意，协议 PM 评估采纳——与数据主权一致）。
- §7.1 抗屏蔽三原则**零改动**：屏蔽仍是接收方本地 opt-out、无全网删除义务、不阻止其他节点展示；裂变转发路径上各节点独立决策（本节点的屏蔽/关档不影响其他节点的收与转）。

### 11.4 fanout 投递形态（信封/卡片零改动下的裂变承载）

**信封约束**（与 §2.3 直达约束并列的形态叠加，T1 §2.1 信封字段零改动）：

| 项 | 值 | 依据 |
|---|---|---|
| `type` | 恒 `"publish"` | T1 §3（复用既有枚举，不新增类型） |
| `payload.delivery` | `"fanout"` | 本文件 payload 语义（T1 §3 明文留白的内容发布域） |
| `ttl` | 发起置 `N`（**默认 2，提案待会签钉死**，§11.7），逐跳 `-1`，`≤ 0` 止转 | T1 §2.1/§4.1（上界 6 = T1 §4.1 `publish` 跳数上限既有值，不重定义） |
| `forward_chain` | 发起 `[src_domain]`，每个转发者转发前**追加自身域名** | T1 §5.1；验签锚定签发前缀（T1 §5.3，KNET-CC-015 已会签定稿）；前缀完整性 `chain[0]==src_domain` fail-closed（T1 §5.3-4） |
| `signature` | 发起方一次签发，**转发者不重签**（中间跳不可归责取舍同 T1 §5.3-5） | T9 §9.3.2 + T1 §5.3（任意跳可验） |
| `payload.target_domain` | **恒 `"*"`（通配）** | 本节定义（见下） |

**target 通配与卡片签名不变性（确认声明）：**

- 裂变转发的是**同一张卡片**：卡片 11 字段（§3.2，不含 `creator_sig`）传输全程不可修改（§5.4「不修改卡片语义字段」既有 MUST 直接适用），转发者不重签卡片、不改写信封 `src_domain`（T1 §5.1——仅向链尾追加，不换发起方）。
- **`creator_sig` 不因转发失效**：卡片级验签输入（§4.3）= `JCS(card 除 creator_sig 外字段) + "\n" + payload.target_domain`；fanout 形态下 `target_domain` 自签发起即恒为通配值 `"*"` 且转发全程不改写，故**任一跳以同一输入重推导验签必过**。签名管线（算法/JCS 规范化/验证步骤 §4.3、§5.3-5/6）**零改动**——仅 `target_domain` 在本形态下取新值 `"*"`（取值语义，非管线改动）。
- 信封签名同理：按 T1 §5.3 签发前缀语义，转发链追加不破坏发起方签名、任意跳可验；信封签名输入中的校验方域名取值与 `payload.target_domain` 一致（即 `"*"`），T9 §9.3.2 覆盖范围结构不变。
- **跨节点重放防护换位说明**（通配不绑定单节点的代价与承接）：定向形态靠 `target_domain` 绑定防卡片跨节点重放（§4.3 原文零改动）；fanout 形态改由三层承接——① **订阅图准入**（未订阅来源的裂变包拒收 403 `not_subscribed`，§11.5）② **T1 §5.2 去重**（`query_id + nonce` 命中 → 409 `replayed`，不重复收录、不重复转发）③ **§3.3 幂等键**（`(creator_domain, content_key)` upsert，重复到达不产生重复收录/放大）。
- **卡片新鲜度对裂变同样适用**（§5.3-3 既有）：`iat` ±5min——转发是即时接力而非延迟重发，延迟到达的过期卡 fail-closed 拒收（符合「转发即背书」的时效语义）。
- **ad_slot 不参与裂变（§8 零改动）**：fanout 信封 MUST NOT 携带 `card_type="ad_slot"` 卡片，违反 → 接收方拒收 400 `invalid_slot`（§8.2 附加校验既有错误码）——广告位维持定向互换（§8.4）现状，不参与裂变。

### 11.5 裂变分发流程（发布 → 粉丝扇出 → 逐跳）

**发起（创作者 A，hop 0）：**

1. 构造 1 张卡片（§3，11 字段不变）+ `creator_sig`（输入尾行取 `"*"`，§11.4）；
2. 构造 1 个信封（§11.4 约束：`ttl=N`、`forward_chain=[A]`、`query_id` CSPRNG、`nonce` 引 T9 §9.3.1）；
3. 对本地**粉丝名册**逐个 `POST /api/v2/push/card`（HTTPS + DoH 寻址同 §9.2 发送方流程）；
4. **计入 T1 §8 每日 3 次 `publish` 配额**（T1 §3/§8 既有口径直接适用——fanout 属泛洪类扩散而非点对点；本节不新增配额语义）。

**逐跳处理（节点 X 收到包，直接上游 = `forward_chain` 链尾 U）：**

```
① 接收准入（fail-closed，§5.2 管线形态分流）：
   传输/信封校验（结构·delivery/target 通配·query_id+nonce 去重 409·验签·反滥用闸 §11.6）
   + 订阅图准入：X 的订阅表含 U（链尾）——否则 403 not_subscribed（未订阅来源不得投喂裂变包）
② 屏蔽压制（三档第 1 档，不收不转）：
   push_blacklist 命中创作者域 → 403 拒收 + MUST NOT 转发（§7.1 既有拒收语义延伸至转发路径）
   T3 -127 命中（发起方或上游）→ 不收不转（T3 §6.2「不转发其包」为其子集，本节 push 语境取不收不转口径）
③ 卡片校验与收录：§5.3 fail-closed → upsert（§3.3）→ 展示（§5.5）
④ 转发决策（口径三查：查订阅表开关 → 查 -127/push_blacklist → 转发）：
   a. 查订阅表开关：X 对 U 的条目 forward_enabled=false → 止（收而不转，三档第 2 档）
   b. 查屏蔽：② 已核（任一命中即不转发——②前移至接收准入是拒收语义的 fail-closed 要求，与本口径语义等价）
   c. 转发条件齐备（ttl-1 > 0 + T1 §5.2 防环：自身不在链/去重未命中/链长约束 + pub_forward_per_min 未超）
      → 向 X 的粉丝名册逐个转发：forward_chain 追加 X、ttl-1、信封 signature 与卡片零改动（同一签名仍有效，§11.4）
```

- `ttl-1 ≤ 0` → 收录但不再转发（N 跳耗尽，天然终点）。
- 多路到达同一包（多个上游转发同一 `query_id`）：首个到达者正常处理，后续去重命中丢弃（T1 §5.2）——不重复收录、不重复放大。
- T1 §4.2 出度上限面向好友链泛洪；订阅裂变的出度 = 粉丝名册规模（自然有界），级联量级由 N 跳（§11.7）+ `pub_forward_per_min`（§11.6）约束——不重定义 T1 §4.2。

### 11.6 限频重定位（反滥用闸，非行为约束）

- **入站限频（§5.2-7 既有值重定位）**：30 次/分/源 = **仅防恶意攻击的反滥用闸**——不是对正常分发行为的行为约束（§COM-01 2026-08-28 口径：push 不做重度协议限频，社交自调节为主——推送多了被屏蔽即自调节）；默认值、节点本地可调；fanout 形态计数键 = 信封 `src_domain`（发起方——转发者中转不消耗自身额度，同一创作者经多路上游到达共享其额度，天然抑制单一发布者放大）。
- **转发侧反滥用上限（本节提案）**：`pub_forward_per_min` = **10 次/分/源**（每节点对同一 `src_domain` 每分钟转发 fanout `publish` 包上限，超限**静默丢弃**不回执）——默认值、节点本地可调、**会签钉死**（先例：014 `cq_forward_per_min`=10，KNET-CC-014 已联合关闭）；定位 = 抑制裂变树级联放大的反滥用闸，非行为约束。
- **T1 §8 每日配额**：维持既有（`publish` 计入每日 3 次），本节不重定位 T1 条款（不越界）。

### 11.7 N 跳默认值提案（会签钉死）

- **提案 N = 2**：量级约束——裂变触达量级随跳数沿订阅图指数放大，2 跳已覆盖「发布者 → 粉丝 → 粉丝的粉丝」核心传播圈；3 跳及以上再放大量级一个数量级，作为节点本地按需放宽区间而非默认（可调区间 `1 ≤ N ≤ 6`，上界 = T1 §4.1 `publish` 跳数上限，不重定义）。配置名建议 `push_fanout_max_hops`（默认 2）。
- 会签对齐点：默认值 2 或 3 由节点 PM 会签钉死（014-④ 默认值先例）。

### 11.8 隐私注记（转发链可见性，同 014 检索隐私明示风格）

- `forward_chain` 明文随包传播：**转发路径上的节点可见自己转发/收到了谁的卡片**（订阅关系与转发路径属明文元数据，非加密内容——介意者可用 §11.3 开关关档收而不转）。

### 11.9 端点与实现建议（草案）

- **订阅管理**：`POST /api/v2/push/subscribe`（body：`{"v":1,"subscriber_domain":"<订阅方域名>"}`）/ `DELETE /api/v2/push/subscribe`——发布方经 DoH/DoT 校验订阅方域名 did:dns 身份（同 §5.3-5 管线精神）后登记入粉丝名册；**免同意**（登记即生效，无批准流）；订阅请求的防滥用（限频等）归节点本地策略。响应：200 `{"status":"subscribed"}` / 400 `invalid_request` / 429 `rate_limited`。
- **裂变接收**：复用 `POST /api/v2/push/card`（`payload.delivery="fanout"` 分流校验，§5.2-3/4 已对齐）。
- **实现建议（非强制）**：订阅表/粉丝名册列映射见 §11.2；转发开关 UI 文案建议明示「转发即背书」语义（节点侧自理）。

---

## 12. 与 T1/T3/T4/T9/did-dns/014 的互引（不重定义清单）

| 引用对象 | 引用内容 | 本文件消费方式 |
|---|---|---|
| T1（flood_protocol.md，KNET-CC-007 会签通过；§5.3 v0.2 随 KNET-CC-015 会签关闭） | §2 泛洪信封 8 字段、§3 `publish` 承载位、§4.1 跳数上限、§5 转发链（§5.1 追加/§5.2 防环去重/§5.3 签发前缀）、§8 配额口径 | §4.1 信封结构整体引用；§2 定义 `publish` 直达变体（ttl=1/不转发/不计配额）；§11.4/§11.5 订阅裂变形态引用（ttl 上界/链追加/防环去重/签发前缀验签）；payload 语义为 T1 明文留白的内容发布域主笔 |
| T9（DECISIONS §9.3 + did-dns §3.5，KNET-CC-011 会签通过并关闭） | §9.3.1 挑战码结构、§9.3.2 签名覆盖范围与 JCS 规范化 | 信封 `signature` 覆盖范围整体引用（第 5 项权重不适用）；卡片级签名（§4.3）为**本文件新契约**，规范化规则（JCS + 固定顺序 + `\n` 分隔）沿用 T9 同一规则，算法 Ed25519 统一红线 |
| T4（multi_candidate_resolution.md，KNET-CC-009 会签通过） | §5 direct/relay 候选与回退 | §6.1 跳转目标的端点解析引用（展示节点侧寻址 creator_domain 时按 T4 排序与回退） |
| T3（trust_weight.md，KNET-CC-008 会签通过） | §2.2 `contacts` 好友边（双轨制之「好友」轨）、§6 `-127` 黑名单（不转发其包/不响应其查询） | §7.2 屏蔽复用；§11.1 双轨制定位 + §11.3/§11.5 屏蔽压制放大（引用不重定义；订阅不进入 T3 取值域） |
| 014（content_query.md，KNET-CC-014 已联合关闭） | §6.2 转发反滥用默认值先例 `cq_forward_per_min`=10 | §11.6 `pub_forward_per_min`=10/分/源 提案对齐同源先例（引用不重定义） |
| did-dns（did-dns-protocol.md） | §2 三记录（v/pk/black）、fp 指纹定义、§3.3 校验规则 | §4.3 验签链、§5.3 第 5 项整体引用（记录格式/指纹算法/黑名单校验不重定义） |

> **纪律:** 以上引用若与被引文件后续修订冲突，以被引文件会签版本为准；本文件不因引用而获得对被引字段的定义权。

---

## 13. 安全考量汇总（fail-closed）

| 威胁 | 缓解 | 落点 |
|---|---|---|
| 伪造卡片（冒名 creator_domain） | `creator_sig` + did:dns fp 指纹链 + DoH/DoT 解析 | §4.3、§5.3-5/6 |
| 卡片跨节点重放 | `target_domain` 纳入卡片签名输入（fanout 通配形态改由订阅图准入 + T1 §5.2 去重 + §3.3 幂等键承接，§11.4） | §4.3、§11.4 |
| 传输重放 | 信封 `nonce` 一次性去重 + `iat` ±5min 新鲜度 | §5.2-5、§5.3-3（引用 T9 §9.3.1） |
| 投毒/灌数据（缺陷 7.1 复发） | fail-closed 全量校验，验不过不收录；入站限频 30/分/源 | §5.3、§5.2-7 |
| 中间人 | 强制 HTTPS + 强制 DoH/DoT（禁明文 53） | §5.2-1 |
| 旧钥/泄露钥签名 | `did:dns:black` 黑名单校验 + `creator_fp` 比对 | §5.3-5 |
| 跳转 URL 注入 | `content_key` 字符集白名单（RFC 3986 unreserved） | §3.2 |
| 卡片超载/巨型包 | 卡片 ≤2 KiB、请求体 ≤8 KiB、字段长度上限 | §3.1/§3.2 |
| 缩略图外链隐私 | 直链可选加载，展示节点可禁用 | §5.4 |
| 裂变树级联放大/风暴（fanout，KNET-CC-017 草案） | N 跳上限（默认 2 提案，上界 T1 §4.1=6）+ `pub_forward_per_min`=10/分/源（提案）+ T1 §5.2 防环/去重 | §11.4/§11.6/§11.7 |
| 转发链元数据可见（fanout 隐私，明示） | `forward_chain` 明文随包——转发路径节点可见自己转发/收到谁的卡片（明示基线，非加密） | §11.8 |

> **去中心化纪律:** 上述防线全部为**接收方本地行为**（验签/限频/黑名单/长度约束），无中心化网关、无注册、无 CA——与 T1 §8 同一纪律。

---

## 14. 自测门禁（交付前自查）

| # | 门禁项 | 本草案结论 |
|---|---|---|
| CP-1 | 需求口径逐条落协议（§COM-01 2026-08-09 八条 ↔ §1.2 P1~P8） | ✅ §1.2 逐条对照（爬虫下线不在本文件范围，仅 P8 说明互补关系且不引用 06 篇） |
| CP-2 | 与 T1 关系清晰（直达变体：ttl=1/不转发/不计配额；publish 语义主笔归属声明） | ✅ §2（T1 §3 明文留白的内容发布域即本文件） |
| CP-3 | 引用 T1 信封结构不重定义 | ✅ §4.1 信封 8 字段整体引用；本文件仅新增 payload 内字段与定向约束 |
| CP-4 | 引用 T9 签名覆盖不重定义 | ✅ §4.2/§12（信封签名覆盖引用 T9 §9.3.2；卡片级签名为本文件新契约，规范化规则沿用同源规则） |
| CP-5 | 抗屏蔽语义明确（opt-out 三原则：本地行为/无全网删除义务/不影响他节点） | ✅ §7.1 三 MUST |
| CP-6 | 端点统一口径 + 现状勘误（/api/v1/publish bug + /api/indexer/publish 已下线） | ✅ §9.1/§9.2 |
| CP-7 | 付费投放留接口不定型（price_hint 恒 null） | ✅ §8.3 |
| CP-8 | `grep -ri aura` 零残留 | ✅（端点/品牌均 kirin 体系，零 aura） |
| CP-9 | 引用编号一致（T1/T4/T9/did-dns §2·§3.5/DECISIONS §9.3/KNET-CC-007·009·011·012/FR4·FR11/10 篇·12 篇） | ✅ |
| CP-10 | fail-closed（无法校验的卡片拒绝收录，明示拒因） | ✅ §5.3 + §13 |
| CP-11（v1.1） | 双模式并存零矛盾（§2 直达语义/§7 抗屏蔽三原则/§8 广告位零改动） | ✅ §2.2 三形态并存 + 并存口径注；§7.1 三 MUST 原文零改动（§11.3 明示裂变不产生全网删除义务）；§8 定向互换零改动（ad_slot 不参与裂变，§11.4） |
| CP-12（v1.1） | 卡片/签名/验签管线零改动（裂变沿用卡片 11 字段 + 两层签名，`creator_sig` 不因转发失效） | ✅ §11.4 确认声明（11 字段传输不可变 §5.4；转发不重签、不换 src；验签输入含 `target_domain`——fanout 下恒 `"*"` 为取值语义非管线改动；T1 §5.3 签发前缀 KNET-CC-015 已会签） |
| CP-13（v1.1） | `grep -ri aura` 零残留（v1.1 新增内容） | ✅（新增文本零 aura；命中仅 CP-8/CP-13 自测命令文本豁免） |
| CP-14（v1.1） | 引用节号准确（T1 §2.1/§3/§4.1/§5.1/§5.2/§5.3/§8、T3 §2.2/§6、014 §6.2 `cq_forward_per_min`、T9/did-dns 沿 §4/§5 既有引用） | ✅（§11 逐处引用 + §12 互引清单 T1/T3/014 行） |

---

## 15. 会签状态与遗留项

| 项 | 归属 | 状态 |
|---|---|---|
| 端点路径 `/api/v2/push/card` + `/api/v2/push/adslot`（跨仓契约） | 本文件 / 节点 PM 会签（KNET-CC-012） | **已会签定稿（KNET-CC-012，2026-08-24）** |
| 跳转 URL 规则 `/view/<content_key>`（内容节点侧须落地公开内容页） | 本文件 / 节点 PM 会签 | **已会签定稿（KNET-CC-012，2026-08-24；波 1 过渡方案 §6.1）** |
| 链接卡片字段定稿（字段集/长度上限/枚举） | 本文件 / 节点 PM 会签 | **已会签定稿（KNET-CC-012，2026-08-24）** |
| indexed_content 重建映射（§10.2 建议，实现细节归节点 PM） | 节点 PM | 建议，不强制 |
| `flood` 投递形态启用排期（依赖节点泛洪引擎实现，fail-closed） | T1 + 节点侧 | 待节点泛洪引擎就绪 |
| 付费投放语义（price_hint 之外的一切付费模型） | 付费结算后另立草案 | **不定型**（§8.3） |
| 代理投递形态（信封发送方 ≠ 卡片创作者） | 未来版本 | 本版不支持（§5.3-2 fail-closed；fanout 转发不属代理投递——src_domain 恒创作者，§11.4） |
| **§11 订阅与裂变分发**（订阅列表/受限裂变 N 跳/转发开关/限频重定位/subscribe 端点） | 本文件 / 节点 PM 会签（KNET-CC-017） | **草案·待会签（未会签单边定稿无效；会签通过前节点侧不得启用 fanout 形态与 subscribe 端点，fail-closed）** |
| N 跳默认值（提案 **2**）与 `pub_forward_per_min`（提案 **10/分/源**） | 本文件 §11.6/§11.7 | 提案值，**会签时钉死**（014-④ 默认值先例）；均为节点本地可调的反滥用闸 |

---

## 16. 版本与变更

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-08-24 | P-DOC 视角首版草案（需求变更 2026-08-09 甲方权威口径）：定向推送 = T1 `publish` 直达变体（§2，ttl=1/不转发/不计配额）+ 链接卡片格式（§3，title/summary/thumbnail_url/creator_domain/content_key/content_type/iat/exp/creator_fp/creator_sig）+ 两层签名（§4，信封引用 T9 §9.3.2 / 卡片级本文件新契约）+ 展示节点 fail-closed 行为（§5）+ 跳转与广告边界（§6，`/view/<content_key>`）+ 抗屏蔽 opt-out 三原则（§7）+ 广告位友情展现与 price_hint 留接口（§8）+ 统一端点 `POST /api/v2/push/card|adslot` 与现状勘误（§9）+ 12 篇 indexed_content 语义调整映射（§10）。**未会签·单边定稿无效**。 |
| v1.0 | 2026-08-24 | **版本行对齐注记（非技术变更，零条款改动）**：即 KNET-CC-012 定稿基线（内容 = v0.1 行所述定稿态，2026-08-24 会签附条件通过·履约·合并 master·联合关闭）——定稿时文件头沿用草案号 0.1 未随定稿重排；v1.1 修订起版本行对齐 1.x 序列（治理记录口径：§COM-01 2026-08-28「content_push.md v1.0→v1.1」）。 |
| v1.1 | 2026-08-28 | **KNET-CC-017 草案（分发模式扩展，未会签·单边定稿无效）**：新增 §11 订阅与裂变分发（11.1 双轨制与两模式并存/11.2 订阅列表/11.3 三档语义/11.4 fanout 形态与签名不变性/11.5 裂变流程/11.6 限频重定位/11.7 N 跳默认值提案 2/11.8 隐私注记/11.9 端点与实现建议）；既有节最小对齐逐处——头部 6 行（版本 1.1/日期/Status 草案子句/Scope/关联编号 +KNET-CC-017·T3·014/注行）、§0 表 +2 行（v1.1 增补行·会签记录草案子句）、§1.1 并存注、§1.3 括注、§2.1 三形态句、§2.2 标题+fanout 行+并存口径注、§5.2-3/4 形态分流与 -7 反滥用闸重定位、§5.3-2 fanout 子句、§9.2 subscribe 端点行+发送方流程括注、§9.3 400 枚举 +`not_fanout_form`/403 +`not_subscribed`、§12 互引清单 T1 行扩 §4.1/§5 引用 + 新增 T3/014 行、§13 安全考量 +2 行（级联放大/转发链可见）与跨节点重放行括注、§14 门禁 +CP-11~14、§15 会签状态 +2 行与代理投递行括注、页脚草案口径；卡片 11 字段/两层签名/§5 管线/§7 三原则/§8 广告位**零改动**。依据 §COM-01 2026-08-28 甲方三项裁定。 |

---

*本文件为定向内容推送与链接卡片协议 **v1.1**（**KNET-CC-017 草案·待会签**：§11 订阅与裂变分发及 v1.1 对齐改动未会签单边定稿无效；v1.0 = KNET-CC-012 已联合关闭定稿基线，其余条款维持定稿效力），跨仓端点/推送语义强制会签项，分发模式扩展须经节点 PM 会签后方可标「定稿」。信封结构引用 T1、签名覆盖引用 T9、好友图与黑名单引用 T3 §2.2/§6、端点解析引用 T4、身份记录引用 did-dns §2/§3.5、转发反滥用先例引用 014 §6.2，均不重定义；付费投放语义不定型（付费结算后定）。*
