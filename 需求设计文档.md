# KirinNet 麒麟网 — 协议需求与设计文档

> **版本:** 1.1（协议版）
> **日期:** 2026-08-01
> **状态:** 草稿（整合自仓库现有文档，供评审）
> **定位:** 本文档是 **KirinNet 协议**的需求与设计总览，覆盖身份协议（DID-DNS）、服务发现（ADRP）、IM 协议、安全模型与客户端生态。**Node 节点实现的需求与设计已拆出，见独立仓库 [KirinNet-Node](https://github.com/kirin-yucall/KirinNet-Node)。**

---

## 1. 文档说明与文档地图

| 主题 | 本文位置 | 详细文档（源） |
|---|---|---|
| 项目总览 | 第 2 章 | `README.md` |
| 需求分析 | 第 4 章 | 各协议文档 |
| 身份协议（DID-DNS） | 第 6.1 章 | `01_Standard/did-dns-protocol.md` |
| 服务发现（ADRP） | 第 6.2 章 | `01_Standard/spec_v1.md`、`draft-*.txt` |
| IM 协议 | 第 6.3 章 | `01_Standard/im_protocol.md` |
| 安全模型 | 第 6.4 章 | `01_Standard/security_model_v1.md` |
| DNS 自动化 | 第 6.1/6.5 章 | `01_Standard/dns_automation.md` |
| SDK / 浏览器生态 | 第 7 章 | `02_Libraries/`、`03_Browser_Extension/`、`04_Chromium_Browser/` |
| 采纳与标准化 | 第 8 章 | `05_Adoption/` |
| 关键决策记录 | 附录 A | `DECISIONS.md` |

---

## 2. 项目概述

### 2.1 愿景与定位

KirinNet（麒麟网）是一个**完全去中心化的身份与节点网络协议**：**域名即身份**，为人和 AI 智能体共享。

- 每个域名就是一个通用、自证的身份容器——无论持有私钥的是人还是 AI 智能体，协议层不做任何区分。
- **不需要中心化 CA、不需要用户注册、不需要密码**：DNS 是身份层，Ed25519 私钥是证明。
- 致力于可信与平等（摒弃算力霸权）、务实与共赢（聚焦实体经济与社会治理真实痛点）、青年担当（中国青年发起的前沿探索）。

### 2.2 核心设计理念

| 原则 | 含义 |
|---|---|
| 域名 = 身份容器 | 域名 + 密钥对 = 自证身份，无需任何人担保 |
| 人与智能体零区分 | 协议层只问"你有私钥吗？"，不问碳基还是硅基 |
| 零 CA 信任链 | DNSSEC/DoH → DNS 公钥 → 指纹验证 → 密码学验证，闭环 |
| 去中心化思维 | 每个用户自跑节点，无中心服务器，不需要限流 |
| Ed25519 唯一密钥类型 | 全系统统一；加密场景 Ed25519→X25519 转换 + HPKE |

### 2.3 与传统体系的对比

| 传统体系 | 问题 | DID-DNS 方案 |
|---|---|---|
| OAuth / OpenID Connect | 假设浏览器背后有人点"允许" | 密码学自证：谁有私钥谁是合法拥有者 |
| API Key | 不记名凭证，泄露即失控 | Ed25519 签名，密码学归属证明 |
| mTLS / x.509 | 依赖中心化 CA 签发和撤销 | DNSSEC/DoH 保护，零 CA |
| KYC 实名认证 | 只适用于法律主体 | 域名=身份，不追问碳基还是硅基 |
| 密码登录 | 数据库拖库即灾难 | 无私钥存储，DNS 即信任锚 |

---

## 3. 术语表

| 术语 | 定义 |
|---|---|
| **DID-DNS** | 以 `did:dns:` 前缀 TXT 记录承载的身份协议（身份声明/公钥/黑名单三记录） |
| **ADRP** | KirinDNS Resolution Protocol：通过 SRV 记录做服务端口发现 + TXT 做身份元数据的解析协议 |
| **指纹（fp）** | `Base64URL(SHA-256(公钥)[0:12])`，16 字符，建立"身份声明→公钥"防篡改验证链 |
| **HPKE** | 混合公钥加密：Ed25519→X25519 转换 + ECDH + HKDF-SHA-256 → AES-256-GCM |
| **用户节点（User Node）** | 协议的实际实现方/服务方，每个用户自跑的 Docker 单镜像（见节点版需求设计文档） |
| **解密端点** | 节点提供的 `POST /.well-known/did-dns/decrypt`，用于自动登录挑战-响应 |

---

## 4. 需求分析

### 4.1 目标用户与场景

| 用户 | 典型场景 | 关键需求 |
|---|---|---|
| AI 智能体 | 智能体间自动发现、认证、通信 | 零人工介入认证、SRV 自动发现、签名合约 |
| 个人用户 | 域名即身份，登录第三方网站 | 自动登录、无密码、无注册 |
| 企业/组织 | 内网服务发现、DAO、多设备 | 政府 CA 实名映射、子域名委托、设备群自证 |
| 隐私需求者 | 异议人士、记者 | 可完全匿名（仅好友网络，不申请 CA） |
| 开发者 | 集成 KirinNet 能力 | 15 语言 SDK、统一 API |

### 4.2 功能需求（FR）

#### FR1 — 身份与 DNS
- 身份声明、公钥、黑名单三类 TXT 记录（`did:dns:` 前缀），单条 ≤ 200 字节避免 UDP 分片
- 自动域名登录验证：服务端到服务端挑战-响应（HPKE 加密挑战码），用户无需手动输入验证码
- 解密端点 `POST /.well-known/did-dns/decrypt`（协议规定 + 节点内置实现）
- 密钥轮换与撤销：旧指纹入 `did:dns:black`，新公钥重新发布
- 服务发现：SRV 记录公布 HTTP/HTTPS/WebSocket 端口，无 SRV 时回退标准端口
- DNS 自动化标准：节点自动更新 A/SRV/TXT 的 API 契约（12 家 DNS 服务商）

#### FR2 — 客户端生态
- SDK：15 种主流语言的 `resolve(domain)` 统一 API（详见 7.1）
- Chrome 扩展：DNS TXT 查询 → 端口发现 → 重定向非标准端口服务（Manifest V3）
- 定制 Chromium：内置扩展打包，核心 DNS 栈零改动

### 4.3 非功能需求（NFR）

| 类别 | 要求 |
|---|---|
| 去中心化 | 无中心服务器；无平台持有用户数据；无统一账号数据库（不存在撞库） |
| 安全 | 零 CA 信任链；端到端加密；密钥可轮换可撤销；攻击单节点 = 物理攻击一个人的代价 |
| 兼容 | DNS 纯增量扩展：不引入新 RR 类型、不改 A/AAAA、无 SRV 时回退标准端口 |
| 国际化 | 提交 IETF（DNSOP 工作组），浏览器/库多生态采纳 |
| 性能（协议约束） | 单条 TXT 记录 ≤ 200 字节；解析缓存按 TTL，miss 回退最近已知值 |

### 4.4 明确不做的事（Out of Scope）

- 不提供中心化注册、CA 签发、平台托管
- IM 协议当前无前向保密（PFS）、无消息签名（列为未来增强）
- 不做多设备同步（节点单节点架构，属节点范围）

---

## 5. 协议架构（四层）

```
┌──────────────────────────────────────────────────────────────┐
│ 协议层  01_Standard：DID-DNS 身份 / ADRP 服务发现 / IM / 安全模型  │
├──────────────────────────────────────────────────────────────┤
│ SDK 层  02_Libraries：15 语言 kirin-dns / kirin-auth 客户端库      │
├──────────────────────────────────────────────────────────────┤
│ 节点层  KirinNet-Node（独立仓库：Docker 单镜像，协议实现方）            │
├──────────────────────────────────────────────────────────────┤
│ 生态层  03 扩展 / 04 Chromium / 05 采纳与标准化                    │
└──────────────────────────────────────────────────────────────┘
```

---

## 6. 协议设计

### 6.1 DID-DNS 身份协议（`01_Standard/did-dns-protocol.md` v1.0）

**记录定义**（TXT，`did:dns:` 前缀，单条 ≤ 200 字节）：

| 前缀 | 记录类型 | 必选 | 字段 |
|---|---|---|---|
| `did:dns:v=...` | 身份声明 | 是 | `v`=1、`fp`=指纹、`n`=昵称(Base64URL)、`g`=性别、`iat`/`exp`=时间窗 |
| `did:dns:pk;...` | 公钥 | 是 | `kty`=ed25519、`pk`=Base64URL 公钥 |
| `did:dns:black;...` | 黑名单 | 否 | `fp`=已撤销指纹（逗号分隔） |

```
mydomain.example.  300  IN  TXT  "did:dns:v=1;fp=AbCdEf1234aaaa;n=QWxpY2U;g=F;iat=1712345678;exp=1712432078"
mydomain.example.  300  IN  TXT  "did:dns:pk;kty=ed25519;pk=MCowBQYDK2VwAyEA..."
mydomain.example.  300  IN  TXT  "did:dns:black;fp=OldKeyFp1,OldKeyFp2"
_kirinnet-ws._tcp.mydomain.example.   300  IN  SRV  0 0 8082 mydomain.example.
```

**指纹验证链**：`fp = Base64URL(SHA-256(公钥)[0:12])` → 声明与公钥防篡改绑定。DNSSEC 签名或可信 DoH 获取为强制要求。

**自动认证流程**（服务端到服务端，零人工）：

```
第三方网站                          用户域名解密服务
  1. DNS 查 TXT（声明/公钥/黑名单）→ 校验 iat/exp 时间窗、fp 不在黑名单、fp 与公钥匹配
  2. 生成挑战码 c（6 位数字 + 时间戳 + HMAC 绑定域名），60 秒一次性
  3. 生成临时 X25519 密钥对，HPKE 加密 c
  4. POST /.well-known/did-dns/decrypt  {enc, ciphertext}
  5. 节点私钥 HPKE 解密 → 返回 {challenge}
  6. 比对 c == c' → 登录成功
```

**解密端点规范**：成功 `200 {challenge}`；错误 `400`（格式）/ `403`（Origin 白名单/解密失败）/ `500`。频率限制：单 IP 每分钟 10 次（默认值，用户可调，无硬上限）。

**安全增强**：DNS 篡改→DNSSEC；重放旧记录→`iat` ±5 分钟新鲜度；密钥泄露→黑名单；中间人→强制 HTTPS；跨站重放→挑战码绑定域名+时间窗；端点滥用→Origin 过滤+解密日志。

**AI 智能体身份模型**（第 9 节）：域名是通用、自证的身份容器，验证只问"你是否持有该域名的私钥？"——智能体可零适配使用全部能力：智能体市场（SRV 自动发现 + 签名合约）、个人 AI 代理（子域名委托 `agent.alice.example`）、链上智能体（`did:dns:dao.example`）、IoT 设备群（每设备一子域名）。

### 6.2 ADRP 服务发现协议（`01_Standard/spec_v1.md` v2.0）

- 三层模型：**SRV 层**（标准 RFC 2782 服务发现）+ **TXT 层**（身份元数据），另 A/AAAA 负责 IP。
- 三个 SRV 服务名：`_kirinnet-http._tcp`、`_kirinnet-https._tcp`、`_kirinnet-ws._tcp`。
- 无 SRV 时回退标准端口：HTTP 80 / HTTPS 443 / WS 80 / WSS 443。
- 安全要求：查询必须走 DoT/DoH（禁明文 53）；SRV/TXT 应被 DNSSEC 覆盖；HTTPS 证书 SAN 必须匹配原域名而非 SRV target。
- 解析缓存：按 TTL 缓存，miss 时回退最近已知值（stale data > no data）。
- IANA：请求注册三个 SRV 服务名。详见 `draft-kirin-yucall-kirindns-adrp-00/01.txt`。

> ⚠️ spec_v1 的身份 TXT 仍为旧格式（`id=;key=;nick=`，推荐 secp256k1），与 DID-DNS 新格式冲突，见 9.2/9.3。

### 6.3 IM 通信协议（`01_Standard/im_protocol.md` v1.0）

- 身份：域名唯一标识；节点启动时生成长期 RSA 密钥对（文档为 4096 位，schema 为 2048 位——见 9.1）。
- 密钥体系：长期密钥（身份验证）+ 好友会话密钥（每好友一对，隔离泄露面）。
- 好友请求两阶段：请求不含密钥（仅意图声明）→ 接受后才交换公钥（见 6.4）。
- 消息：接收方会话公钥加密（RSA-OAEP），仅收方可解密。
- 局限：无 PFS、无消息签名（未来增强：ECDH 前向保密 + ECDSA 签名）。

> ⚠️ 本文档仍使用 `/aura/*` 旧路径与 RSA 体系，**与 DID-DNS/Ed25519 体系不一致**，见 9.1、9.4。

### 6.4 安全模型（`01_Standard/security_model_v1.md` v1.0）

**三层信任模型**：

| 层 | 机制 | 解决 |
|---|---|---|
| 好友层（最强） | 好友间 RSA 密钥对，首次交换后锁定 | 身份冒充、中间人攻击、信任传导 |
| 公共层 | 政府 CA 签发的域名证书 | 证明"域名背后是真实的人"，但不暗示可信 |
| 物理层 | 政府 CA 认证 | 监管合规、物理追责、陌生人最低信任锚 |

- **密钥交换五种方式**（信任递减）：CA 直连（TLS）> 线下扫码 > 好友介绍（≤2 跳）> 临时握手页（5 分钟时效）> 公开页面。
- **密钥轮换**：新公钥必须由旧私钥签名（reason: voluntary/compromised/device_lost）；旧密钥丢失则退回物理层线下重换。
- **分布式入侵检测**：心跳（正常 30s；3 次未收到→away，30 次→offline）+ RSA 密钥异常交叉验证 + 流量骤降（>80% 持续 6h + 密钥异常→劫持告警），预警仅广播给共同好友。
- **故障隔离**：攻击单节点 = 物理攻击一个人的代价；不存在撞库、不存在平台拖库。

### 6.5 DNS 自动化标准（`01_Standard/dns_automation.md` v2.1）

- 职责分离：SRV 管 **where**（端口），TXT 管 **who**（身份），A/AAAA 管 **which IP**。
- 更新端点 `POST https://dns.kirinnet.org/api/v2/update`：`X-DNS-Token` + `X-Domain` 认证，payload 含 `srv_records[]` + `did_dns_records{}` + `ttl`。
- 节点侧更新循环：TTL 的 80% 间隔刷新（300s TTL → 240s），不做指数退避（DNS 最终一致，错过 = 陈旧而非断连）。
- 自托管 DNS：可指向自建 CoreDNS（自定义 plugin），同一 API 契约，mTLS 或 localhost 认证。

### 6.6 加密原语约定

| 用途 | 原语 | 说明 |
|---|---|---|
| 身份签名/指纹 | Ed25519 | 协议统一密钥类型 |
| 传输加密 | HPKE：Ed25519→X25519 + ECDH + HKDF-SHA-256 → AES-256-GCM | 挑战码加密、内容加密推送 |
| DNS 查询传输 | DoT（RFC 7858）/ DoH（RFC 8484） | ADRP 查询强制要求，禁明文 53 |

---

## 7. 客户端与生态设计

### 7.1 SDK（15 语言）

| # | 语言 | 状态 |
|---|---|---|
| 1 | JavaScript | ✅ 已测试，npm 已发布 |
| 2 | Python | ✅ 已测试，PyPI 已发布 |
| 3 | Go | ✅ 语法通过（GitHub: kirin-dns-go） |
| 4 | Rust | ✅ 语法通过（GitHub: kirin-dns-rs） |
| 5 | C | ✅ 已测试（gcc） |
| 6 | C++ | ✅ 已测试（g++17） |
| 7-15 | C#/Java/Kotlin/Dart/Ruby/Swift/PHP/Lua/TypeScript | 语法通过，待实测（TS 为 JS 包内 `.d.ts`） |

统一 API：`resolve(domain)` → `{identity, pubkey_verified, service.ws_port}`。注：宣称 15 语言，磁盘实现目录 14 个（TS 仅是类型定义），见 9.5。

### 7.2 浏览器扩展（03_Browser_Extension）

Manifest V3：权限 `webRequest` / `webRequestBlocking` / `storage` / `declarativeNetRequestWithHostAccess`，host `<all_urls>`，后台 service_worker。功能：DNS TXT 查询 → 端口发现 → 重定向到非标准端口服务。

### 7.3 定制 Chromium（04_Chromium_Browser）

- 目标 Chromium 135；**核心 DNS 栈零改动**，扩展作为内置 component extension 打包。
- 变更仅限：新增扩展目录 + 修改 `BUILD.gn` / `extension_ids.json`；构建约需 100GB 磁盘、32GB+ RAM。

---

## 8. 采纳与标准化

### 8.1 IETF 路线图（`05_Adoption/rfc_draft.md`）

- 将 ADRP 作为 Internet-Draft 提交（已生成 `draft-*-00/01.txt`，占位日期需替换，TXT/SRV 未定前未提交）。
- 主推 **DNSOP** 工作组（备选 HTTPBIS）；类别 Informational/BCP；总时间线 6-12 个月。
- 里程碑：12 个月发布 RFC；18 个月内 ≥1 家浏览器 flag 支持。

### 8.2 市场推广（`05_Adoption/go_to_market.md`）

- 四阶段 36 个月：启动（1-3 月）→ 教育（4-9 月）→ 合作（10-18 月）→ 主流（18-36 月）。
- 目标受众：Web3 开发者（主）/ 企业 IT / 隐私倡导者；预算总计 $19,005；成功指标逐阶段爬升（扩展安装 500→10 万）。

### 8.3 演示场景（`05_Adoption/demo_sites.md`）

5 个可复现用例：IPFS 网关 dApp 端口发现、企业内网反代门户、自定义 DoT/DoH 端口、单 IP 多服务、Web3 节点动态端口轮换。

---

## 9. 已识别的问题与不一致（需评审决策）

> 本章为协议仓库视角。节点相关的已知问题见 [KirinNet-Node](https://github.com/kirin-yucall/KirinNet-Node) 仓库的《需求设计文档》第 9 章。

| # | 问题 | 冲突双方 | 影响 | 建议处理 |
|---|---|---|---|---|
| 9.1 | **密钥体系三套并存** | DID-DNS/README：Ed25519；IM 协议/安全模型：RSA-2048/4096；ADRP spec_v1：secp256k1 | 协议无法互操作 | 统一为 Ed25519（IM 消息加密改 HPKE），更新 im_protocol/security_model/spec_v1 |
| 9.2 | **TXT 格式两代** | spec_v1.md（ADRP）：旧格式 `id=;key=;nick=;ipfs=`；did-dns-protocol：新格式 `did:dns:v=...;pk;black` | 客户端解析规则冲突 | 全量迁移到 `did:dns:` 格式，废弃旧格式描述 |
| 9.3 | **spec_v1 落后于 did-dns** | spec_v1.md（ADRP v2.0）与 did-dns-protocol v1.0 定义冲突（见 9.1/9.2） | IETF 草案内容基于旧协议 | 以 did-dns-protocol 为基线修订草案 |
| 9.4 | **品牌残留 `/aura/*`** | DECISIONS：旧名 AuraDNS/AuraNet 已全量替换；im_protocol.md、04_Chromium 仍用 `aura` | 违反文档规范，语义混乱 | 全量替换为 `kirin` |
| 9.5 | **15 语言 vs 14 目录** | README/DECISIONS：15 语言；磁盘：14 个实现目录（TS 仅 .d.ts） | 宣传与交付不符 | 修正描述为 14 语言 + TS 类型定义，或补 TS 独立实现 |
| 9.6 | **compatibility.md 为空** | 文件存在但 0 字节 | 文档承诺未兑现 | 填充内容或删除 |

---

## 附录 A — 协议相关关键决策（摘自 DECISIONS.md）

1. 命名：KirinNet（麒麟网）/ KirinDNS (ADRP) / `kirin-dns` / `kirin-yucall`；旧名 AuraDNS/AuraNet 应已全量替换（见 9.4）。
2. 协议 TXT vs SRV：**审计结论为文档/代码不一致，待用户决策**（对应 9.1/9.2/9.3）。SRV 存端口、TXT 存身份的双记录模型为既定方向。
3. SDK：JS/Python 已发布，Go/Rust 独立仓库，C/C++ 已测，其余待实测。
4. IETF：草案已生成未提交（占位日期未替换）。
5. 安全：两阶段好友请求、政府 CA 只证"物理身份=域名"、分布式入侵检测。
6. 去中心化：不需要限流、DNS 即信任锚。
7. 文档规范：中文、patch 逐节迭代、API 路径必须用 `kirin`（零 `aura`）。

## 附录 B — 本仓库文档索引

| 路径 | 说明 | 状态 |
|---|---|---|
| `README.md` | 协议项目总览 | ✅ |
| `DECISIONS.md` | 架构决策记录（含节点决策，拆分后精简） | ✅ |
| `需求设计文档.md` | 本文档（协议版） | ✅ |
| `01_Standard/did-dns-protocol.md` | DID-DNS 身份协议 v1.0 | ✅ 基线 |
| `01_Standard/spec_v1.md` | ADRP 解析协议 v2.0 | ⚠️ 旧格式，需对齐 |
| `01_Standard/dns_automation.md` | DNS 自动化标准 v2.1 | ✅ |
| `01_Standard/im_protocol.md` | IM 协议 v1.0 | ⚠️ 旧品牌/旧密钥 |
| `01_Standard/security_model_v1.md` | 安全威胁模型 v1.0 | ⚠️ RSA 体系，需对齐 |
| `01_Standard/compatibility.md` | 兼容性说明 | ❌ 空文件 |
| `01_Standard/draft-*-adrp-00/01.txt` | IETF 草案 | ⚠️ 待替换占位日期 |
| `02_Libraries/` | 15 语言 SDK | ⚠️ 待实测补齐 |
| `03_Browser_Extension/` | Chrome 扩展（MV3） | ✅ |
| `04_Chromium_Browser/` | 定制 Chromium 构建说明 | ✅ |
| `05_Adoption/rfc_draft.md` | IETF 标准化路线图 | ✅ |
| `05_Adoption/go_to_market.md` | 市场推广策略 | ✅ |
| `05_Adoption/demo_sites.md` | 演示场景 | ✅ |
