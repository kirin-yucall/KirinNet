# KirinNet 麒麟网 — 协议需求与设计文档

> **版本:** 1.5（需求对齐 v4）
> **日期:** 2026-08-01
> **状态:** 草稿（已完成四轮需求对齐，结论已落盘；协议细节设计待细化）
> **定位:** 本文档是 **KirinNet 协议**的需求与设计总览，覆盖身份模型（域名/钱包双模式）、身份验证（签名质询 + HPKE）、服务发现（ADRP）、受限泛洪传播（计数模式 → 信任模式）、分布式 DNS（IM 层信任解析）、IM 协议、安全模型与客户端生态。**Node 节点实现的需求与设计见独立仓库 [KirinNet-Node](https://github.com/kirin-yucall/KirinNet-Node)。**

---

## 1. 文档说明与文档地图

| 主题 | 本文位置 | 详细文档（源） |
|---|---|---|
| 项目总览 | 第 2 章 | `README.md` |
| 需求分析（已对齐） | 第 4 章 | 2026-08-01 三轮需求对齐结论 |
| 身份协议（DID-DNS） | 第 6.1 章 | `01_Standard/did-dns-protocol.md` |
| 服务发现（ADRP） | 第 6.2 章 | `01_Standard/spec_v1.md`、`draft-*.txt` |
| 受限泛洪传播（新） | 第 6.3 章 | 待细化（本次对齐新增） |
| 分布式 DNS（IM 层，新） | 第 6.4 章 | 待细化（本次对齐新增） |
| IM 协议 | 第 6.5 章 | `01_Standard/im_protocol.md`（待改造） |
| 安全模型 | 第 6.6 章 | `01_Standard/security_model_v1.md` |
| DNS 自动化 | 第 6.7 章 | `01_Standard/dns_automation.md` |
| SDK / 浏览器生态 | 第 7 章 | `02_Libraries/`、`03_Browser_Extension/`、`04_Chromium_Browser/` |
| 关键决策记录 | 附录 A | `DECISIONS.md` |

---

## 2. 项目概述

### 2.1 愿景与定位

KirinNet（麒麟网）是一个**完全去中心化的身份与节点网络协议**：**私钥即身份，域名即寻址标签**，为人和 AI 智能体共享。

- **钱包地址即身份**：用户利用现有加密货币基建（XRP 优先）获得全球唯一私钥，身份 ID 就是钱包地址（公钥编码）——不需要注册、不需要任何中心化机构。
- 域名是可选的人类可读标签（寻址用），**域名冲突合法**，由去中心化节点群的信任权重解决，法律兜底。
- **引导期计数防投毒**：域名发布需经计数泛洪（1000 万阈值 → 共识时间 → 全网开关关闭计数），防止投毒泛滥。
- **运营期社交防污染**：解析由 IM 好友链泛洪携带信任权重驱动，社交节点自我修复。
- **不需要中心化 CA、不需要用户注册、不需要密码**：DNS 是身份层，Ed25519 私钥是证明。
- 致力于可信与平等（摒弃算力霸权）、务实与共赢（聚焦实体经济与社会治理真实痛点）、青年担当（中国青年发起的前沿探索）。

### 2.2 核心设计理念（含需求对齐结论）

| 原则 | 含义 |
|---|---|
| **双模式身份（无全网分界）** | 可发布域名以前：**域名即身份**（公钥放传统 DNS TXT）；可发布域名以后：**钱包私钥即身份**（公钥放分布式 DNS TXT）；两种模式长期共存，节点自行演进 |
| 域名 = 寻址标签 | 域名是可选的人类可读标签；**允许不同的人发布相同域名**，冲突由信任权重解决 |
| 人与智能体零区分 | 协议层只问"你有私钥吗？"，不问碳基还是硅基 |
| 零 CA 信任链 | DNSSEC/DoH → DNS 公钥 → 指纹验证 → 密码学验证，闭环 |
| **身份 + 发布 + IM 一体** | 同一个协议承载身份、内容发布与即时通信，非分离模块 |
| **IM = 分布式 DNS** | IM 好友列表即分布式 DNS 解析网络，基于好友链泛洪 + 六度分隔 |
| **两阶段演进** | 引导期（计数模式，防投毒）→ 共识时间开关 → 运营期（信任模式，防污染） |
| **社交自我修复** | 信任权重由节点手动维护（IM 列表），转发链带权重，负权重降分，形成防污染解析 |
| **域名归属去中心化** | 由去中心化节点群（信任共识）决定域名归属，非中心化注册/唯一性机制 |
| 去中心化思维 | 不用限流、不用中心化注册；节点轻量化（本地映射表基于 IM 列表） |
| Ed25519 唯一密钥类型 | 全系统统一；加密场景 Ed25519→X25519 转换 + HPKE |

### 2.3 与传统体系的对比

| 传统体系 | 问题 | KirinNet 方案 |
|---|---|---|
| OAuth / OpenID Connect | 假设浏览器背后有人点"允许" | 密码学自证：谁有私钥谁是合法拥有者 |
| API Key | 不记名凭证，泄露即失控 | Ed25519 签名，密码学归属证明 |
| mTLS / x.509 | 依赖中心化 CA 签发和撤销 | DNSSEC/DoH 保护，零 CA |
| KYC 实名认证 | 只适用于法律主体 | 私钥=身份，不追问碳基还是硅基 |
| 密码登录 | 数据库拖库即灾难 | 无私钥存储，DNS 即信任锚 |
| 中心化域名注册 | 域名唯一性由注册局裁决 | 允许同名，节点群信任权重评分，法律兜底 |
| 中心化 DNS 防污染 | 依赖根区/权威区的治理 | 引导期计数防投毒 + 运营期社交信任权重防污染 |

---

## 3. 术语表

| 术语 | 定义 |
|---|---|
| **钱包地址即身份** | 运营期身份 ID = 钱包地址（Ed25519 公钥编码，XRP 优先）；私钥由现有加密货币基建生成/保管 |
| **签名质询-应答** | 身份验证：校验方发送随机信息 → 受验方私钥签名返回 → 校验方公钥验签（两阶段统一） |
| **DID-DNS** | 以 `did:dns:` 前缀 TXT 记录承载的身份协议（身份声明/公钥/黑名单三记录） |
| **ADRP** | KirinDNS Resolution Protocol：通过 SRV 记录做服务端口发现 + TXT 做身份元数据的解析协议 |
| **指纹（fp）** | `Base64URL(SHA-256(公钥)[0:12])`，16 字符，建立"身份声明→公钥"防篡改验证链 |
| **HPKE** | 混合公钥加密：Ed25519→X25519 转换 + ECDH + HKDF-SHA-256 → AES-256-GCM |
| **受限泛洪** | 带跳数上限的泛洪传播：入网宣告 15 跳；好友链泛洪用于解析/发布（非广播） |
| **计数包** | 引导期域名发布携带的计数宣告包（域名+公钥+签名），节点计数防投毒 |
| **共识时间戳** | 计数达 1000 万后由多点协商发布的全网时间戳；触发全网开关（关闭计数），并暂存为发布时序裁定依据 |
| **转发链** | 泛洪包每跳记录的转发者域名列表，兼作防环依据 |
| **信任权重** | 节点对域名/发布者的评分（-127 黑名单起，正向评分）；相同域名可获不同评分 |
| **IM 列表** | 节点手动维护的信任列表：**不止好友，还允许存放常用网址**，均可维护权重 |
| **钱包级负权重** | 将某钱包地址拉入负权重后，其发布的域名与转发的包在解析中分数降低 |
| **多候选解析** | 同名域名查询返回多个候选（各带信任权重），客户端按权重排序自行选择 |
| **IM 黑名单** | 信任权重 -127 的域名：不转发其 DNS 查询、不提供其 IP 查询结果 |
| **用户节点（User Node）** | 协议的实际实现方/服务方，每个用户自跑的 Docker 单镜像（见 KirinNet-Node） |

---

## 4. 需求分析（已对齐）

> 本章为 2026-08-01 三轮需求对齐的结论，已确认，可继续补充。

### 4.1 目标用户与场景

| 用户 | 典型场景 | 关键需求 |
|---|---|---|
| AI 智能体 | 智能体间自动发现、认证、通信 | 零人工介入认证、好友链解析、签名合约 |
| 个人用户 | 钱包即身份，登录第三方网站 | 自动登录、无密码、无注册 |
| 加密货币用户 | 已有钱包（XRP 等）直接接入 | 私钥复用、零门槛加入 |
| 企业/组织 | 内网服务发现、DAO、多设备 | 政府 CA 实名映射、子域名委托、设备群自证 |
| 隐私需求者 | 异议人士、记者 | 可完全匿名（仅好友网络，不申请 CA） |
| 开发者 | 集成 KirinNet 能力 | 15 语言 SDK、统一 API |

### 4.2 功能需求（FR）

#### FR1 — 身份（双模式 + 签名质询）
- **双模式身份（无全网分界，长期共存）**：
  - 可发布域名以前：**域名即身份**——用户把公钥放置在传统 DNS 的 TXT 解析记录（`did:dns:pk`）
  - 可发布域名以后：**钱包私钥即身份**——钱包发布域名，公钥放置在分布式 DNS 的 TXT 记录（同为 `did:dns:pk`，经 IM 好友链解析得到）
  - 两阶段都依据 **DNS 发布的公钥信息**（传统 DNS / 分布式 DNS 两种通道）
- **身份验证（统一，两阶段一致）**：
  - **签名质询-应答（核心）**：校验方发送随机信息 → 受验方用私钥签名返回 → 校验方用公钥验签 → 完成身份验证
  - **HPKE 加密挑战（保留原方案，并存）**：解密端点 `POST /.well-known/did-dns/decrypt` 与挑战码流程继续保留
- **钱包地址即身份（运营期）**：身份 ID = 钱包地址（Ed25519 公钥编码，XRP 优先）；利用现有加密货币基建获得唯一私钥，无需注册，无需有钱
- 三类 TXT 记录（身份声明/公钥/黑名单），`did:dns:` 前缀，单条 ≤ 200 字节避免 UDP 分片；**全量迁移 `did:dns:` 格式，废弃旧格式（`id=;key=;nick=`）**
- 指纹验证链：`fp = Base64URL(SHA-256(公钥)[0:12])`，防公钥替换
- 密钥轮换与撤销：旧指纹入 `did:dns:black`，新公钥重新发布
- **DoH/DoT 强制（必须项）**：身份记录解析必须走 DoT（RFC 7858）/ DoH（RFC 8484）；明文 DNS（UDP/TCP 53）返回的身份记录**拒绝或降级告警**，不得静默采信；支持 DNSSEC 校验的客户端应验证签名
- 限频默认 10/min/IP（可调，无硬上限）

#### FR2 — 服务发现（ADRP）
- SRV 三服务名（`_kirinnet-http/https/ws._tcp`）公布端口；无 SRV 回退标准端口（80/443）
- 查询强制 DoT/DoH（禁明文 53）；SRV/TXT 应被 DNSSEC 覆盖
- 按 TTL 缓存解析结果，miss 回退最近已知值

#### FR3 — 受限泛洪传播（计数模式 → 信任模式）
- **入网宣告**：新节点首次联网发受限泛洪包（15 次转发上限），载荷 = **域名 + Ed25519 公钥 + 签名**
- **转发链**：每跳转发者记录自己的域名；转发链兼作防环依据（链上节点不再转发）
- **引导期计数（防投毒）**：
  - 新域名发布走**计数泛洪**（计数包），节点计数
  - 计数达 **1000 万阈值** → **多点协商**发布全网共识时间戳
  - **共识时间触发后所有节点开启开关，不再返回计数包**（计数功能关闭，域名发布回归普通宣告）
- **泛洪配额**：每节点每天最多 **3 次**用户自主非系统需求的泛洪（DNS 查询/内容发布）；系统需求（入网宣告等）不受限
- **超限惩罚**：超限自动进入 IM 黑名单，信任权重 **-127**；不向其转发 DNS 查询结果、不提供其 IP 查询结果

#### FR4 — 分布式 DNS（IM 层信任解析）
- **IM = 分布式 DNS 节点服务器**：解析基于社交 IM 列表，六度分隔可达任何节点
- **双通道更新**：域名解析更新不只发布到 DNS 服务商，还通过 IM 底层消息传递给好友，存于好友节点
- **好友链泛洪解析**：解析泛洪基于好友链（非广播）；持有未过期结果的好友返回结果
- **信任权重体系（社交自我修复防污染）**：
  - **IM 列表**：不止好友，还允许存放**常用网址**，均可手动维护权重
  - **好友间信任权重不同**（分级）
  - **转发链天然携带权重**：经好友链转发时自动带权重分
  - **冲突由用户决策**：遇到同名冲突，用户给出评分；可决定将某**钱包地址拉进负权重（钱包级）**
  - **负权重降分**：负权重钱包的转发链分数降低，影响解析结果
- **域名冲突合法**：允许不同的人发布相同域名；**多候选 + 权重排序**——查询同名域名返回多个候选（各带信任权重），客户端按权重排序自行选择
- **域名归属去中心化**：由去中心化节点群的信任权重决定归属；**纠纷留给法律**
- **共识时间戳（暂存）**：作为发布时序裁定的依据（用户裁定），VDF 机制暂存待用
- **本地映射表**：完全基于 IM 列表；仅少量缓存自身常用的非好友数据（节点轻量化）

#### FR5 — 内容发布
- 发布走**受限泛洪**传播（计入每天 3 次配额），与 DNS 查询同一通道
- 大文件/媒体传播方式待细化（见 6.9 待细化项）

#### FR6 — IM 协议
- 钱包地址/域名即身份；两阶段好友请求（先意图声明，接受后交换密钥）
- 每好友独立会话密钥（隔离泄露面）
- **端点 `/aura/*` 全量改名 `/kirin/*`**；密钥统一 Ed25519（加密走 HPKE）
- **PFS（前向保密）纳入 v1**：会话密钥 ECDH 轮换

#### FR7 — 安全模型
- 三层信任保留：好友层（密钥锁定）/ 公共层（政府 CA 实名映射）/ 物理层（监管追责）
- **好友层密钥统一 Ed25519**（签名 + HPKE 加密）
- 心跳（30s）+ 分布式入侵检测（密钥异常交叉验证 + 流量骤降）保留
- 密钥轮换须旧密钥签名；**信任权重体系：-127（黑名单）+ 正向评分（好友分级、常用网址、钱包级负权重）**

#### FR8 — DNS 自动化
- 更新端点 `POST /api/v2/update` 保留（引导期）；更新循环 TTL×80%
- 中心化 DNS 服务商 **12 家扩展为 15 家**
- **定位：引导期依赖 + 长期并行兜底**——后期不可能消灭传统 DNS，互相兼容更务实

#### FR9 — 客户端生态
- SDK：保持 **15 种语言宣称**（JS/Python/C/C++ 已实测，Go/Rust 语法通过，其余待实测），**需求设计完成后实现**
- Chrome 扩展（MV3，端口发现+重定向）；定制 Chromium（零核心改动）——解析格式跟随 `did:dns:`

### 4.3 非功能需求（NFR）

| 类别 | 要求 |
|---|---|
| 去中心化 | 无中心服务器；无平台持有用户数据；无统一账号数据库（不存在撞库）；域名归属由节点群信任共识决定 |
| 安全 | 零 CA 信任链；端到端加密；密钥可轮换可撤销；攻击单节点 = 物理攻击一个人的代价 |
| 防投毒 | 引导期计数（1000 万阈值 + 共识时间开关）限制域名发布泛滥 |
| 防污染 | 运营期信任权重（IM 列表 + 转发链权重 + 钱包级负权重）自我修复污染 |
| 兼容 | DNS 纯增量扩展：不引入新 RR 类型、不改 A/AAAA、无 SRV 时回退标准端口；与传统 DNS 长期共存互操作 |
| 轻量化 | 节点本地映射表基于 IM 列表，少量缓存非好友数据 |
| 性能（协议约束） | 单条 TXT 记录 ≤ 200 字节；解析缓存按 TTL，miss 回退最近已知值 |
| 标准化 | **暂缓 IETF 提交**，先完善协议与实现 |

### 4.4 明确不做的事（Out of Scope）

- 不提供中心化注册、CA 签发、平台托管
- **不做域名唯一性验证**（允许同名，信任权重解决冲突）
- **不做域名纠纷裁决**（留给法律；共识时间戳仅作用户裁定依据）
- **不做 VDF 唯一性裁定**（VDF 时间戳暂存待用，后续可能作为发布时序依据）
- 计数机制**仅限引导期**（共识时间后关闭，不长期保留）
- 不提供 Web UI 密码找回（节点范围）
- 前期不消灭传统 DNS——互相兼容（务实取向）
- 暂缓 IETF 标准化（先完善协议与实现）

---

## 5. 协议架构（四层）

```
┌──────────────────────────────────────────────────────────────┐
│ 协议层  01_Standard：DID-DNS 身份 / ADRP 服务发现 / IM / 安全模型 │
│         受限泛洪传播（计数→信任）/ 分布式 DNS（待细化）            │
├──────────────────────────────────────────────────────────────┤
│ SDK 层  02_Libraries：15 语言 kirin-dns / kirin-auth 客户端库      │
├──────────────────────────────────────────────────────────────┤
│ 节点层  KirinNet-Node（独立仓库：Docker 单镜像，协议实现方）        │
├──────────────────────────────────────────────────────────────┤
│ 生态层  03 扩展 / 04 Chromium / 05 采纳与标准化（暂缓）            │
└──────────────────────────────────────────────────────────────┘
```

---

## 6. 协议设计

### 6.1 身份模型与 DID-DNS（双模式 + 签名质询）

**身份模型（双模式，无全网分界）：**

```
模式一：可发布域名以前 → 域名即身份
  公钥放置在传统 DNS 的 TXT 解析记录（did:dns:pk）
  身份锚点 = 域名（解析经中心化 DNS 服务商）

模式二：可发布域名以后 → 钱包私钥即身份
  钱包发布域名，公钥放置在分布式 DNS 的 TXT 记录（同为 did:dns:pk）
  身份锚点 = 钱包私钥（解析经 IM 好友链泛洪）

两种模式长期共存，节点自行演进（无全网分界时点）
两阶段都依据 DNS 发布的公钥信息（传统 DNS / 分布式 DNS 两种通道）
```

- **身份 ID**：模式一为域名；模式二为钱包地址（Ed25519 公钥经钱包协议编码，XRP 优先，Base58 地址格式），私钥由现有加密货币基建（硬件钱包/助记词）生成保管——零注册、零门槛
- **域名 = 寻址标签**：域名发布 `did:dns:` TXT 记录（指纹/公钥/黑名单），**允许同名共存**
- **冲突解决**：节点群对同名域名给出信任权重评分（多候选 + 权重排序），用户决策，法律兜底
- **did:dns 记录**：以域名发布到传统 DNS（模式一）与 IM 好友/分布式 DNS（模式二），指纹链验证不变

**身份验证（统一，两阶段一致）：**

```
校验方                                    受验方
  1. DNS 解析（传统或分布式）获取公钥
     → 校验 fp = SHA-256(pk)[0:12] 与声明一致
  2. 发送随机信息（挑战）───────────────→
  3.                             用私钥签名随机信息
     ←───────────────────────────────── 返回签名
  4. 用公钥验签 → 验证通过
```

- **签名质询-应答（核心）**：随机信息格式（Nonce + 时间戳防重放）待细化（T9）
- **HPKE 加密挑战（保留原方案，并存）**：解密端点 `POST /.well-known/did-dns/decrypt` 与挑战码流程继续保留，作为另一条身份验证通道

**记录定义**（TXT，`did:dns:` 前缀，单条 ≤ 200 字节）：

| 前缀 | 记录类型 | 必选 | 字段 |
|---|---|---|---|
| `did:dns:v=...` | 身份声明 | 是 | `v`=1、`fp`=指纹、`n`=昵称(Base64URL)、`g`=性别、`iat`/`exp`=时间窗 |
| `did:dns:pk;...` | 公钥 | 是 | `kty`=ed25519、`pk`=Base64URL 公钥 |
| `did:dns:black;...` | 黑名单 | 否 | `fp`=已撤销指纹（逗号分隔） |

```
mydomain.example.  300  IN  TXT  "did:dns:v=1;fp=AbCdEf1234aaaa;n=QWxpY2U;g=F;iat=1712345678;exp=1712432078"
mydomain.example.  300  IN  TXT  "did:dns:pk;kty=ed25519;pk=MCowBQYDK2VwAyEA..."
mydomain.example.  300  IN  TXT  "did:dns:black;fp=OldKeyFp1,OldKeyFp2"
_kirinnet-ws._tcp.mydomain.example.   300  IN  SRV  0 0 8082 mydomain.example.
```

**指纹验证链**：`fp = Base64URL(SHA-256(公钥)[0:12])` → 声明与公钥防篡改绑定。DNSSEC 签名或可信 DoH 获取为强制要求。

**自动认证流程**（服务端到服务端，零人工；与签名质询并存）：

```
第三方网站                          用户域名解密服务
  1. DNS 查 TXT（声明/公钥/黑名单）→ 校验 iat/exp 时间窗、fp 不在黑名单、fp 与公钥匹配
  2. 生成挑战码 c（6 位数字 + 时间戳 + HMAC 绑定域名），60 秒一次性
  3. 生成临时 X25519 密钥对，HPKE 加密 c
  4. POST /.well-known/did-dns/decrypt  {enc, ciphertext}
  5. 节点私钥 HPKE 解密 → 返回 {challenge}
  6. 比对 c == c' → 登录成功
```

**解密端点规范**：成功 `200 {challenge}`；错误 `400`（格式）/ `403`（Origin 白名单/解密失败）/ `500`。频率限制：单 IP 每分钟 10 次（默认值，可调，无硬上限）。

**安全增强**：DNS 篡改→DNSSEC；重放旧记录→`iat` ±5 分钟新鲜度；密钥泄露→黑名单；中间人→强制 HTTPS；跨站重放→挑战码绑定域名+时间窗；端点滥用→Origin 过滤+解密日志；**DNS 投毒（无加密解析）→ 客户端强制 DoH/DoT（必须项），明文 53 身份记录拒绝/降级告警，TOFU 警示 + 跨通道交叉验证（详见 `security_model_v1.md` 第七章）**。

**AI 智能体身份模型**：钱包地址是通用、自证的身份容器，验证只问"你是否持有该私钥？"——智能体可零适配使用全部能力：智能体市场（自动发现 + 签名合约）、个人 AI 代理（子域名委托 `agent.alice.example`）、链上智能体（`did:dns:dao.example`）、IoT 设备群（每设备一子域名）。

### 6.2 ADRP 服务发现协议（`01_Standard/spec_v1.md` v2.0）

- 三层模型：**SRV 层**（标准 RFC 2782 服务发现）+ **TXT 层**（身份元数据），另 A/AAAA 负责 IP。
- 三个 SRV 服务名：`_kirinnet-http._tcp`、`_kirinnet-https._tcp`、`_kirinnet-ws._tcp`。
- 无 SRV 时回退标准端口：HTTP 80 / HTTPS 443 / WS 80 / WSS 443。
- 安全要求：查询必须走 DoT/DoH（禁明文 53）；SRV/TXT 应被 DNSSEC 覆盖；HTTPS 证书 SAN 必须匹配原域名而非 SRV target。
- 解析缓存：按 TTL 缓存，miss 时回退最近已知值（stale data > no data）。
- **已对齐决策**：TXT 身份部分全量迁移 `did:dns:` 格式（废弃 `id=;key=;nick=`）；spec_v1 以此为基线修订。
- IANA：请求注册三个 SRV 服务名。详见 `draft-kirin-yucall-kirindns-adrp-00/01.txt`。

### 6.3 受限泛洪传播（计数模式 → 信任模式，设计待细化）

**两阶段演进：**

```
阶段一：引导期（计数模式）— 防投毒
  新节点发布域名 → 计数泛洪（计数包：域名 + Ed25519 公钥 + 签名）
  → 每跳转发者记录自己的域名（转发链，防环）
  → 节点计数（计数包被返回/计数）
  → 计数达 1000 万阈值 → 多点协商共识时间戳
  → 共识时间触发：所有节点开启开关，不再返回计数包（计数功能关闭）

阶段二：运营期（信任模式）— 社交防污染
  域名解析/发布 → 好友链泛洪（非广播），转发链天然携带信任权重
  → 权重来源：IM 列表（好友 + 常用网址，手动维护）
  → 好友间信任权重不同（分级）
  → 同名冲突 → 用户决策并评分 → 可将钱包地址拉进钱包级负权重
  → 负权重钱包的转发链分数降低 → 影响解析结果
  → 由社交节点自我修复维护的 DNS 防污染解析
```

**泛洪配额与惩罚：**

- 每节点每天最多 **3 次**用户自主非系统需求的泛洪（DNS 查询、内容发布）；系统需求（入网宣告、计数泛洪等）不受限
- 超限 → 自动进入 IM 黑名单：信任权重 **-127**；不向其转发 DNS 查询结果、不提供其 IP 查询结果

**共识时间戳（暂存）**：触发开关后全网不再返回计数包；时间戳本身保留，作为发布时序裁定的依据（用户裁定），VDF 机制暂存待用（后续可能用于发布时序、关联信任权重分与节点商城交易模块）。

**设计待细化**：计数包格式与计数规则、多点协商协议（阈值验证/时间戳收敛/开关传播）、末端回传语义、VDF 预留位。

### 6.4 分布式 DNS — IM 层信任解析（设计待细化）

- **IM = 分布式 DNS 节点服务器**：每个节点的社交 IM 列表构成解析网络
- **双通道更新**：解析更新 → ① 发布到中心化 DNS 服务商（引导期）② 通过 IM 底层消息传给好友，存于好友节点
- **解析流程**：向好友链发起受限泛洪（非广播）→ 基于六度分隔可命中任何节点 → 持有未过期结果的好友返回结果
- **信任权重体系（防污染核心）**：
  - **IM 列表结构**：好友 + 常用网址，节点手动维护权重
  - **好友分级**：好友之间的信任权重不同
  - **转发链权重**：经好友链转发时天然携带权重分
  - **用户决策**：同名冲突时用户给出评分；可把某钱包地址拉进**钱包级负权重**
  - **负权重降分**：负权重钱包发布的域名、转发的包在解析链上分数降低，影响解析结果
- **域名冲突与归属**：
  - 允许不同的人发布相同域名（同名合法）
  - **多候选 + 权重排序**：同名查询返回多个候选（各带信任权重），客户端按权重排序自行选择
  - **域名归属由去中心化节点群决定**（信任共识）；**纠纷留给法律**
- **本地映射表**：完全基于 IM 列表；仅少量缓存自身常用的非好友数据（节点轻量化）

**设计待细化**：好友链泛洪消息格式、缓存 TTL 与过期策略、信任权重传播与聚合规则（好友分级/网址权重/钱包级负权重）、多候选结果的携带格式、六度分隔可达性保证。

### 6.5 IM 通信协议（`01_Standard/im_protocol.md` v1.0，待改造）

- 身份：钱包地址唯一标识；节点启动时生成长期密钥对（**统一 Ed25519，废弃 RSA**）。
- 密钥体系：长期密钥（身份验证）+ 好友会话密钥（每好友一对，隔离泄露面）。
- 好友请求两阶段：请求不含密钥（仅意图声明）→ 接受后才交换公钥。
- 消息加密：**HPKE（Ed25519→X25519 + ECDH + AES-256-GCM）**，仅收方可解密。
- **PFS（前向保密）纳入 v1**：会话密钥 ECDH 轮换。
- **端点路径 `/aura/*` 全量改名 `/kirin/*`**。

> 本文档需按以上结论改造（原文档为 RSA-4096 + `/aura/*` + 无 PFS）。

### 6.6 安全模型（`01_Standard/security_model_v1.md` v1.0，待改造）

**三层信任模型**：

| 层 | 机制 | 解决 |
|---|---|---|
| 好友层（最强） | 好友间密钥对（**统一 Ed25519**），首次交换后锁定 | 身份冒充、中间人攻击、信任传导 |
| 公共层 | 政府 CA 签发的域名证书 | 证明"域名背后是真实的人"，但不暗示可信 |
| 物理层 | 政府 CA 认证 | 监管合规、物理追责、陌生人最低信任锚 |

- **密钥交换五种方式**（信任递减）：CA 直连（TLS）> 线下扫码 > 好友介绍（≤2 跳）> 临时握手页（5 分钟时效）> 公开页面。
- **密钥轮换**：新公钥必须由旧私钥签名（reason: voluntary/compromised/device_lost）；旧密钥丢失则退回物理层线下重换。
- **分布式入侵检测**：心跳（正常 30s；3 次未收到→away，30 次→offline）+ 密钥异常交叉验证 + 流量骤降（>80% 持续 6h + 密钥异常→劫持告警），预警仅广播给共同好友。
- **故障隔离**：攻击单节点 = 物理攻击一个人的代价；不存在撞库、不存在平台拖库。
- **信任权重**：-127（黑名单）起，好友分级 + 常用网址 + 钱包级负权重（见 6.4）。

### 6.7 DNS 自动化标准（`01_Standard/dns_automation.md` v2.1）

- 职责分离：SRV 管 **where**（端口），TXT 管 **who**（身份），A/AAAA 管 **which IP**。
- 更新端点 `POST https://dns.kirinnet.org/api/v2/update`：`X-DNS-Token` + `X-Domain` 认证，payload 含 `srv_records[]` + `did_dns_records{}` + `ttl`。
- 节点侧更新循环：TTL 的 80% 间隔刷新（300s TTL → 240s），不做指数退避（DNS 最终一致，错过 = 陈旧而非断连）。
- 自托管 DNS：可指向自建 CoreDNS（自定义 plugin），同一 API 契约，mTLS 或 localhost 认证。
- **已对齐决策**：服务商 12 家扩展为 15 家；引导期依赖 + 长期并行兜底（与传统 DNS 共存互操作）。

### 6.8 加密原语约定

| 用途 | 原语 | 说明 |
|---|---|---|
| 身份签名/指纹 | Ed25519 | 协议统一密钥类型（含好友层） |
| 身份验证（核心） | 签名质询-应答 | 随机信息 → 私钥签名 → 公钥验签 |
| 身份验证（并存） | HPKE 加密挑战 + 解密端点 | 原方案保留（RFC 9180：Ed25519→X25519 + ECDH + HKDF-SHA-256 → AES-256-GCM） |
| 身份 ID 编码 | 钱包地址（XRP 优先） | 利用现有加密货币基建生成/保管私钥（模式二） |
| 传输加密 | HPKE：Ed25519→X25519 + ECDH + HKDF-SHA-256 → AES-256-GCM | IM 消息、内容加密推送 |
| DNS 查询传输 | DoT（RFC 7858）/ DoH（RFC 8484） | ADRP 查询强制要求，禁明文 53 |
| 前向保密 | 会话密钥 ECDH 轮换 | IM v1 纳入 |
| 共识时间戳 | 多点协商（算法待细化） | 触发开关；发布时序裁定依据；VDF 暂存 |

### 6.9 待细化项（对齐后遗留，进入协议设计阶段处理）

| # | 项 | 说明 |
|---|---|---|
| T1 | 泛洪消息格式 | TTL 字段、转发链结构、包类型（宣告/计数/解析/发布）、末端回传语义 |
| T2 | 钱包身份适配 | XRP 地址编码与 Ed25519 公钥映射、其他链扩展位 |
| T3 | 信任权重体系 | IM 列表结构（好友分级 + 常用网址）、钱包级负权重、转发链权重携带、聚合规则 |
| T4 | 多候选解析格式 | 同名多候选的返回格式、权重携带、客户端选择策略 |
| T5 | 大文件/媒体传播 | 发布走泛洪后，大内容的分发方式 |
| T6 | 分布式 DNS 细节 | 好友链泛洪格式、缓存 TTL、六度分隔可达性保证 |
| T7 | 浏览器扩展/Chromium | 跟随 `did:dns:` 新格式的解析实现 |
| T8 | 计数与共识时间 | 计数包格式与计数规则、1000 万阈值验证、多点协商协议、开关传播、VDF 预留位（后续可能关联节点商城交易模块） |
| T9 | 签名质询细节 | 随机信息格式（Nonce + 时间戳防重放）、签名覆盖范围、与 HPKE 挑战通道的分工 |

---

## 7. 客户端与生态设计

### 7.1 SDK（15 语言）

| # | 语言 | 状态 |
|---|---|---|
| 1 | JavaScript | ✅ 已测试，npm 已发布 |
| 2 | Python | ✅ 已测试，PyPI 已发布 |
| 3 | Go | ✅ 语法通过（GitHub: kirin-dns-go） |
| 4 | Rust | ✅ 语法通过（GitHub: kirin-dns-rs） |
| 5 | C | ✅ 已测试（gcc） |
| 6 | C++ | ✅ 已测试（g++17） |
| 7-15 | C#/Java/Kotlin/Dart/Ruby/Swift/PHP/Lua/TypeScript | 语法通过，待实测（TS 为 JS 包内 `.d.ts`） |

统一 API：`resolve(domain)` → `{identity, pubkey_verified, service.ws_port}`。**已对齐决策：保持 15 语言宣称，需求设计完成后实现。**

### 7.2 浏览器扩展（03_Browser_Extension）

Manifest V3：权限 `webRequest` / `webRequestBlocking` / `storage` / `declarativeNetRequestWithHostAccess`，host `<all_urls>`，后台 service_worker。功能：DNS TXT 查询 → 端口发现 → 重定向到非标准端口服务。**解析格式需跟随 `did:dns:` 新格式（T7）。**

### 7.3 定制 Chromium（04_Chromium_Browser）

- 目标 Chromium 135；**核心 DNS 栈零改动**，扩展作为内置 component extension 打包。
- 变更仅限：新增扩展目录 + 修改 `BUILD.gn` / `extension_ids.json`；构建约需 100GB 磁盘、32GB+ RAM。

---

## 8. 采纳与标准化（暂缓）

- **已对齐决策：暂缓 IETF 提交**，先完善协议与实现。
- 路线图（`05_Adoption/rfc_draft.md`）：DNSOP 工作组、Informational/BCP、6-12 个月时间线——**待协议成型后重启**。
- 市场推广（`05_Adoption/go_to_market.md`）：四阶段 36 个月、预算 $19,005——**待协议稳定后启动**。
- 演示场景（`05_Adoption/demo_sites.md`）：5 个可复现用例——**需按新协议更新**。

---

## 9. 已识别的问题与不一致（含对齐后状态）

> 本章为协议仓库视角。节点相关的已知问题见 [KirinNet-Node](https://github.com/kirin-yucall/KirinNet-Node) 仓库的《需求设计文档》第 9 章。

| # | 问题 | 冲突双方 | 对齐后状态 | 建议处理 |
|---|---|---|---|---|
| 9.1 | 密钥体系三套并存 | DID-DNS：Ed25519；IM/安全模型：RSA；ADRP：secp256k1 | ✅ **已决策：统一 Ed25519** | 修订 im_protocol/security_model/spec_v1 |
| 9.2 | TXT 格式两代 | spec_v1 旧格式 vs did-dns 新格式 | ✅ **已决策：全量迁移 `did:dns:`** | 修订 spec_v1 与草案 |
| 9.3 | spec_v1 落后于 did-dns | ADRP v2.0 vs DID-DNS v1.0 | ✅ **已决策：以 did-dns 为基线** | 修订草案 |
| 9.4 | 品牌残留 `/aura/*` | DECISIONS 称已替换；im_protocol/Chromium 仍用 aura | ✅ **已决策：改 `/kirin/*`** | 替换 im_protocol/Chromium |
| 9.5 | 15 语言 vs 14 目录 | README：15；磁盘：14（TS 仅 .d.ts） | ✅ **已决策：保持 15 宣称，需求完成后实现** | 待实测补齐 |
| 9.6 | compatibility.md 为空 | 文件存在但 0 字节 | ⏳ 未决 | 填充内容或删除 |
| 9.7 | IETF 草案基于旧协议 | 草案内容为旧格式 | ✅ **已决策：暂缓标准化** | 协议成型后按新基线重写 |
| 9.8 | 计数/VDF 机制反复（v1.2→v1.3→v1.4） | v1.2 设计收集+阈值+VDF；v1.3 取消；v1.4 恢复计数（限引导期） | ✅ **已决策（v1.4）：恢复计数防投毒，限引导期；VDF 时间戳暂存待用** | 已按 v1.4 更新本文档 |
| 9.9 | 身份锚点演进（v1.5 新增） | v1.3/1.4：钱包地址即身份；v1.5：双模式（可发布域名前域名即身份，后钱包私钥即身份，无全网分界） | ✅ **已决策（v1.5）：双模式长期共存；身份验证统一为签名质询-应答，HPKE 原方案保留并存** | 已按 v1.5 更新本文档 |
| 9.10 | DNS 投毒威胁（2026-08-01 新增） | 无 CA + 无 DoH 时明文 DNS 可伪造身份记录（指纹链自洽，无法发现） | ✅ **已决策：强制 DoH/DoT（必须项）；明文 53 身份记录拒绝/降级告警；TOFU 警示 + 跨通道交叉验证；HPKE 通道优先** | 已补充 `security_model_v1.md` 第七章 |

---

## 附录 A — 协议关键决策（DECISIONS.md + 2026-08-01 需求对齐 v3）

1. 命名：KirinNet（麒麟网）/ KirinDNS (ADRP) / `kirin-dns` / `kirin-yucall`；旧名 AuraDNS/AuraNet（残留待清，见 9.4）。
2. 密钥体系：**统一 Ed25519**（签名 + HPKE 加密，公钥加密私钥解密等价 RSA）；IM 与好友层同步统一；PFS 纳入 v1。
3. TXT 格式：**全量迁移 `did:dns:`**，废弃 `id=;key=;nick=`。
4. 协议一体：**身份 + 发布 + IM 为同一协议**。
5. **身份模型（v2→v4 演进）**：**双模式身份，无全网分界**——可发布域名以前：**域名即身份**（公钥放传统 DNS TXT）；可发布域名以后：**钱包私钥即身份**（XRP 优先，利用现有加密货币基建获得唯一私钥，公钥放分布式 DNS TXT）；两种模式长期共存，节点自行演进。
6. **身份验证（v4）**：统一为**签名质询-应答**（校验方发随机信息 → 受验方私钥签名 → 校验方公钥验签）；**HPKE 加密挑战 + 解密端点保留原方案并存**。
6. **域名归属（v2）**：由**去中心化节点群的信任权重**决定；同名域名**多候选 + 权重排序**；**纠纷留给法律**。
7. **引导期计数（v3 恢复）**：域名发布走计数泛洪防投毒；计数达 **1000 万** → **多点协商**共识时间戳 → **全网开启开关，不再返回计数包**（计数限引导期）。
8. **VDF（v3 暂存）**：共识时间戳保留，作为发布时序裁定的依据（用户裁定）；VDF 机制暂存待用；信任权重分后续关联节点商城交易模块。
9. **信任权重体系（v3）**：IM 列表（好友 + 常用网址）手动维护；好友间权重不同；转发链天然带权重；冲突由用户决策评分；**钱包级负权重**降低解析分数 → 社交节点自我修复的防污染 DNS。
10. 泛洪规则：入网宣告 15 跳广播泛洪；解析/发布为好友链泛洪（非广播）；每节点每天 **3 次**用户自主泛洪配额；超限进 IM 黑名单（权重 -127，不转发/不响应）。
11. 分布式 DNS：**IM = 分布式 DNS**，双通道更新（DNS 服务商 + IM 好友消息），好友链泛洪 + 六度分隔解析，本地映射表基于 IM 列表（轻量化）。
12. DNS 服务商：12 家扩展为 **15 家**；引导期依赖 + 长期并行兜底（不消灭传统 DNS，互操作务实）。
13. SDK：保持 15 语言宣称，需求设计完成后实现。
14. 标准化：**暂缓 IETF**，先完善协议与实现。
15. 文档规范：中文、patch 逐节迭代、API 路径必须用 `kirin`（零 `aura`）。
16. **DNS 解析安全（2026-08-01）**：**强制 DoH/DoT（必须项）**——身份记录解析禁止明文 53；明文返回的身份记录拒绝或降级告警；TOFU 首次接触警示；跨通道交叉验证（传统 DNS vs 分布式 DNS）；自动登录优先 HPKE 解密端点通道。威胁分析见 `security_model_v1.md` 第七章。

## 附录 B — 本仓库文档索引

| 路径 | 说明 | 状态 |
|---|---|---|
| `README.md` | 协议项目总览 | ⚠️ 待按 v1.4 对齐更新（计数/信任权重） |
| `DECISIONS.md` | 架构决策记录 | ⚠️ 待按 v1.4 对齐更新 |
| `需求设计文档.md` | 本文档（需求已对齐） | ✅ v1.4 |
| `01_Standard/did-dns-protocol.md` | DID-DNS 身份协议 v1.0 | ⚠️ 需补钱包身份模型 |
| `01_Standard/spec_v1.md` | ADRP 解析协议 v2.0 | ⚠️ 旧格式，需按 9.2/9.3 修订 |
| `01_Standard/dns_automation.md` | DNS 自动化标准 v2.1 | ⚠️ 服务商数待更新 |
| `01_Standard/im_protocol.md` | IM 协议 v1.0 | ⚠️ 需按 6.5 改造 |
| `01_Standard/security_model_v1.md` | 安全威胁模型 v1.0 | ⚠️ 需按 6.6 改造 |
| `01_Standard/compatibility.md` | 兼容性说明 | ❌ 空文件 |
| `01_Standard/draft-*-adrp-*.txt` | IETF 草案 | ⏸ 暂缓 |
| `02_Libraries/` | 15 语言 SDK | ⚠️ 需求完成后实现 |
| `03_Browser_Extension/` | Chrome 扩展（MV3） | ⚠️ 跟随新格式 |
| `04_Chromium_Browser/` | 定制 Chromium 构建说明 | ⚠️ 品牌残留 |
| `05_Adoption/` | 标准化/GTM/演示 | ⏸ 暂缓 |
